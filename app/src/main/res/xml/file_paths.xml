<?xml version="1.0" encoding="utf-8"?>
<!-- Only the artifacts folder is shareable. Nothing else in the app's storage
     is exposed, so a share intent cannot be turned into a way out for anything
     private. -->
<paths>
    <files-path name="artifacts" path="artifacts/" />
</paths>

# JARVIS OS — Progress

> Living status. Update this whenever something ships.
> Snapshot: session branch `claude/phone-glitch-investigation-g6dnhk` @ `9cfc4fa` · `main` @ `9cfc4fa` · last green build: `jarvis-debug-apk` present for `9cfc4fa` · updated 2026-08-23
> **✅ GREEN AND MERGED at `9cfc4fa`.** `b753359` failed on `HomeScreen.kt:720`/`:726`, `Unresolved reference 'amplitude'` — the lambda was threaded `MainActivity` → `JarvisApp` → `HomeContent` and stopped, but `HomeContent` delegates to a nested `HeroSection`, which is where `HudOrb` and `VoiceWave` actually live. No language trap: I grepped the call sites and never checked that the function *containing* each declared the parameter. Fixed in `60452fa`, with a mechanical containment pass added, and `main` is fast-forwarded.
> **🐌 FOUND IT: the app was recomposing ENTIRELY, several times a second, forever.** `VoiceUiState` carried `amplitude` — the mic level — and the mic reports RMS many times a second whenever it is open, which is nearly always because the wake word is always being listened for. Every callback built a new state object, and the whole app composes under one `val state by engine.state` at the top of `setContent`, so **every one of them recomposed the entire tree**: home, settings, chat, a list mid-scroll. That is what *"the app isn't smooth idk why"* was, and it explains the shape of it — not one screen, not one gesture, everything, always, with no trigger. Amplitude is its own `MutableFloatState` now and reaches the two Canvases that draw it as a `() -> Float` **called inside the draw**, so a level change invalidates one draw and recomposes nothing. A test asserts it never comes back onto `VoiceUiState`.
> **🧹 `OrbDetail` FIXED THE RINGS AND MISSED THE MOTES.** Its own comment records 14,600 allocations a frame from rebuilding ring geometry into fresh lists — and `drawMotes` went on calling `spherePoints` every frame anyway, which builds a `List` and a `Vec3` per point, then three more objects per point for the rotation and projection. ~560 allocations a frame for geometry that never changes. Unit-sphere positions are cached per count and the transform is written out in floats; the loop allocates nothing, and one sin/cos pair now serves the whole field.
> **✅ GREEN AND MERGED at `8673930`.** `bcf46c1` failed on three compile errors, two of them **the `IntSize` trap for the third time**: `PointerInputScope.size` is an `IntSize`, `DrawScope.size` is a `Size`. Twice before it was `minDimension`, which only one of them has. This time it was `size.width`, which **both** have as different types — and `Int * Float` is a `Float`, so every arithmetic use compiled and it stayed invisible until the value crossed a function boundary wanting a `Float`. The fix is not another `.toFloat()`: there is one named `span` property now, documented with all three failures, and reaching for raw `size` inside a `pointerInput` is the review smell. Third error: `SkyCache` was `internal` while its return type was private in the file. Fixed in `d645286`, docs in `8673930`, both green with the artifact, and `main` fast-forwarded.
> **📊 WHAT THE GATE SAW.** `Cosmos.kt` — the whole generator rewrite, the surface patterns, the star temperature sequence, all the distinctness tests — compiled and passed off-device in seconds, with **zero** defects. `OrbUniverse.kt` and `ThemeBackdrop.kt` are Compose, which `jvmcheck` cannot resolve without `dl.google.com`, so CI is the first compiler they meet — and **all three faults were in them**. That is the case for pushing decisions into the pure layer, as evidence rather than preference.
> **🔍 FIXED: nothing zoomed, and only the scene could be shoved about.** `view`, `yaw` and `pitch` were `remember(galaxy, chosen, world) { … }` — **keyed**, so each stage change built a NEW state object, while `pointerInput(Unit)` went on writing to the one it captured at first composition. `pan` had no keys, so it was the only state a gesture could still reach: *"instead of zoomable, they displace, i can move them around my screen"* is that sentence exactly. Third instance of one rule (after the stale `stars` list and a captured `palette`): nothing a gesture touches may be reached through a keyed `remember`. Keyless state, reset by a `LaunchedEffect`.
> **🫥 AND IT HID EVERYTHING ELSE.** *"none of the fixes you claimed make are evident"* — right, and this is why. Landmarks reveal above a view of 1.15, worlds grow with the view, surface detail only reads close up. Stuck at 1.0 forever, three of the four layers were showing their dullest frame with no way past it.
> **🤏 ONE FINGER MOVES, TWO FINGERS ZOOM.** `detectTransformGestures` reports the centroid's drift as pan on the same frame it reports a zoom, and two fingers never pull apart symmetrically — so a pinch was scaling and shoving at once. A single pointer reports a zoom of exactly 1, so gating pan on that costs a drag nothing and stops a pinch doing two jobs.
> **🪐 THE PLANETS: three real faults, none of them the drawing.** (1) Nothing was **clipped to the disc** — the old comment claimed "clipped by construction", which is true of where a feature is centred and false of where it ends; round caps bulged past the limb and the ice caps were sized at 1.5x the chord, wider than the planet. (2) **Light ran under the surface instead of over it**, so the night side was as bright as the day side and nothing read as a sphere; the terminator is now a pass over the finished surface with a specular bloom. (3) Worlds were **about twenty pixels across** — eight kinds of surface detail drawn into something too small to show any of it. "Low quality" was the scale.
> **🔭 MOST OF A SYSTEM WAS OFF THE SCREEN, and the tests found it.** Orbits compound at 1.30-1.52x to reach **8.5** against a frame of 1, so the inner two or three were drawn and the rest were outside the screen and unreachable — you cannot zoom out to find them, because pinching out past the minimum is how you leave. Orbits are now a fraction of the system's own width. Two rejected attempts on the way, both caught off-device in seconds: plain division put the innermost world **inside its sun**, and a power-law compression fixed that while breaking the existing rule against evenly spaced rings. The two constraints leave exactly one window — at most **seven** worlds at **1.26-1.34x** — and it is now pinned by tests at both ends. `Cosmos.kt` is **34 tests**.
> **🌍 THE GENERATOR COULD ONLY EVER MAKE EIGHT WORLDS.** `PlanetKind` decided the material AND the drawing routine — eight kinds, eight `when` branches, one picture each — so hue and albedo were a tint on a fixed image and *no amount of extra seeding could have helped*. That is why the first attempt failed and the complaint came back. `SurfacePattern` is now a **separate axis**: the material picks the palette and the plausible patterns, the pattern draws the surface, and on top go caps, cloud, storms, rings, atmosphere, impact rays and how much of a broken world is left. Six independent rolls must agree before two worlds look alike.
> **🎚️ THREE AXES WERE BEING GENERATED AND THROWN AWAY.** `tilt` had existed since the type was written and was **never once read**, so every planet in the universe was posed identically. Ring systems differed only in band count; the *opening* varies now, edge-on to face-on, with a division that moves. And `spin` puts the marks in different places on two worlds that share a pattern.
> **⭐ STARS WERE SIX COLOURS, ALL WHITE IN THE MIDDLE.** Colour was `lerp(accent, highlight, kind.heat)` — the app's two theme colours mixed by kind, giving a whole galaxy six possible star colours — and `point()` drew a pure white disc at every call site, so the colour lived only in the faint halo. New pure `starInk` puts stars on a temperature sequence with a **per-class** spread (a white dwarf is always near-white, so it varies in saturation and lightness rather than hue — the test caught that). Spike count and lean are per star. Same rule as dimensions: past the orb, the theme stops.
> **⚡ THE LAG WAS THE SKY.** An infinite transition behind *every* destination redrawing a full screen at 60fps: ~205 `drawCircle`s plus ~15 flares, and each flare allocates **three gradient shaders** — forty-five shader allocations a frame, for a background, while the foreground tried to scroll. The faint majority now batch into a dozen `drawPoints` calls (same pixels, a fifteenth of the draw list) and no longer twinkle, which is truer as well as cheaper. And the clocks only run where nothing scrolls: at 150 and 38 second periods nobody ever *saw* the sky move behind a settings list — they only felt it.
> **✋ FIXED: the scene could be shoved off centre.** The pan limit was a flat 0.45 of the frame at any zoom, so content that fitted perfectly could still be dragged into a corner. The allowance is the **overhang** now — at a view of 1 the limit is zero — and zooming out reclaims the travel it lent, so nothing stays stranded.
> **🔬 TEXTURE THAT RESOLVES.** Closing on a world now brings up surface grain, drawn in relief and batched, that was not there before — a world merely drawn *larger* gives nothing back for the approach.
> **🪐 FOUR LAYERS YOU CAN TOUCH.** orb → galaxy → **a star's SYSTEM** → **a world, and things on it**. Layer three used to be the endless self-similar dive; it is now a sun with worlds on real orbits (spaced geometrically, each ~1.4x the one inside it, because evenly spaced orbits read as a target) and a rubble belt. Layer four is the payoff for travelling: ten kinds of landmark suited to the world they sit on — a derelict station over a shattered planet says what happened there — held back until the view has closed in, so arriving and *finding* are two separate moments. `Cosmos.kt` is now **30 tests**.
> **🖱️ FIXED: stars could not be clicked, and the code was running the whole time.** `pointerInput(Unit)` is created once and NEVER restarted, so its lambda captured `stars` **by value** from the composition that made it — when no galaxy was open and the list was empty. `galaxy != null` read true (that goes through a `MutableState` and stays live), so the branch ran, the tap landed, and it hit-tested an empty list every time. `rememberUpdatedState` for everything a gesture reads.
> **🔄 EVERYTHING IS INTERACTIVE.** One vocabulary across all four layers — pinch to zoom, drag to move or turn, tap to go in, pinch out to leave. Dragging the orb adds yaw and pitch to every ring's **tilt** rather than rotating the finished picture, so the assembly genuinely turns in space and the galaxies riding the rings go round the back. The turn reaches ring geometry, beads and the hit test from one piece of arithmetic.
> **🌌 THE ORB OPENS INTO ITSELF.** Pinch it and you get *your* orb — the genuine renderer at the genuine theme, 1.55x, no wordmark — and the lights riding its rings turn out to be **galaxies**, one per ring, so Arc's five open onto five and Nebula's three onto three. *"count the number of moving orbs on the Jarvis (main orb) and build galaxies according to that"*: the count is not ours to pick. Each galaxy holds 5–9 stars along its arms; each star is a dimension; each dimension grows worlds. Surfacing walks back up one stage at a time rather than dumping the user on the home screen from six levels inside a star inside a galaxy.
> **🎨 FIXED: "the dimensions 60-70% look the same".** They were all drawn in the app theme's two colours — so however different the geometry underneath, the eye saw one place, because colour is the strongest signal a place has and it was constant. Past the orb the theme now stops entirely (*"a dimension of the forge not necessarily be it's theme colours"*). New pure `Cosmos.kt` generates four inks per place from its own seed, with star kind anchoring the family and the seed moving inside it. **19 tests** pin the properties that matter: gas never within 0.16 of the arm on the wheel (adjacent hues were exactly what flattened a dimension into one wash), a family never scattering more than 0.20, twenty-five places of one kind giving twenty-plus distinct hues, and nothing coming out grey or black.
> **🪐 WORLDS, NOT DOTS.** Eight planet kinds, each with a surface *drawn* differently rather than tinted differently — banded gas giants with their storm, cratered rock with lit rims and cast shadow, ocean coastlines under cloud, lava crust with heat glowing through the cracks, ice caps, ring systems drawn in halves so the planet sits inside them, shattered debris still on the old orbit. All lit from one side by a shared `shade()`, because features shaded independently look like stickers. Each star kind grows only worlds that suit it: somewhere lava and ice are equally likely has no climate.
> **🔭 TWO BUGS THE TESTS FOUND.** (1) Stars placed along galaxy arms land on top of each other where two arms cross — measured 0.126 of the frame apart, close enough that nearest-wins hit testing gives one to the other and **a dimension becomes permanently unreachable**; they are relaxed apart deterministically now, with vertical distance weighted because a phone is twice as tall as it is wide. (2) `UniverseMath.starMap()` had become dead production code kept alive only by its own tests — a fixture that is the last caller of the thing it tests is not testing anything, and it had stopped covering the path that ships. Deleting it is how the overlap surfaced.
> **💥 FIXED: Settings crashed on scrolling into the backgrounds.** `JarvisPalette.Forge` has id `"forge"`; `BackdropStyle.ForgeFloor` **also** has id `"forge"`; both `items()` calls sat in one `LazyColumn` keyed on `it.id`. A duplicate key throws, and only when the second one composes — exactly the moment you scroll past the themes. Keys are namespaced (`theme:` / `backdrop:`) so the collision is impossible rather than merely absent: two enums in separate files were never going to be checked against each other by eye.
> **🐌 FIXED: the picker lagged, for the second time and the same reason.** Eleven live `ThemeBackdrop`s, each an infinite transition invalidating a Canvas at 60fps with a 150-star field and four additive blooms. This is the identical fault the theme picker had with seven live orbs — recorded in `HudOrb` and repeated anyway. New `thumbnail` flag: frozen clocks (created only when used, since an ignored transition still schedules frames) at a quarter detail.
> **🖼️ BACKGROUNDS ARE A GRID NOW.** Three across, portrait tiles, name beneath — as the reference screenshot showed. Ten full-width rows meant ten screens of scrolling to compare two scenes that differ by *structure*; they have to be seen side by side. Themes stay full-width: a live orb plus a sentence does not survive a third of the width.
> **⭐ THE STARS DID NOT LOOK LIKE STARS.** They were large soft radial gradients with an outline circle each — an out-of-focus light with a targeting reticle on it. Three structural faults: no hard core (a star is a point source; the core is now a sharp near-white disc at a third the radius), no diffraction spikes (the four-point cross is the most recognisable feature of a bright star; every kind has one now bar the protostar, where nothing escapes the cloud), and the rings, which were most of why the chart read as UI. The sky behind them was an opaque black rectangle with 150 identical dots: now gas you look *through*, a galactic band on the diagonal, and 420 stars over three depths with brightness squared so most are faint and a few genuinely bright.
> **🌌 THE DIMENSIONS WERE ALL THE SAME, AND SEED-BRANCHING WAS NEVER GOING TO FIX IT.** Nine stars gave nine different sets of random numbers drawn from **one set of ranges** — and a spiral is a spiral whatever numbers built it. Character comes from the ranges, not the rolls inside them. Each `StarKind` now carries its dimension's physics (which shapes form, density, body count, tempo, reach) and biases every colour by its heat: blue giant vast and empty, red dwarf crowded and slow, binary everything in twos, pulsar twice as fast as anything else, protostar densest, white dwarf emptiest. Pinned by tests comparing whole dimensions rather than single shells.
> **↩️ ORBIT AND FORGE PUT BACK, THEN FORGE IMPROVED PROPERLY.** Both rebuilds *replaced* a design instead of improving it. Orbit is the original rings again and is drawn **smaller** rather than narrowed — its widest ring is 1.55 orb radii and `1.55 × 0.86 = 1.33` against a frame 1.0 across, so no tilt tweak saves it; `fitFor` draws it at 0.657 of the half-frame, same shape, all of it visible, wordmark scaled to match. Forge is the six fine rings and 24 spokes verbatim (a *filigree* is fine metal; three heavy bands and a molten lump threw away the thing being improved), plus two additions it never had: **beads** of light running the wires, depth-shaded so they pass visibly round the back, and a heat gradient from near-white at the hub to deep gold at the rim.
> **☁️ NEBULA HAD NO HARD EDGE ANYWHERE**, which is why it read as a smudge — two faint rings, a five-veil core and seven lobes merging into one flat wash. Now three brighter narrower rings, a compact three-veil core to settle on, four lobes, and the heaviest mote field of the four.
> **🖼️ TEN BACKGROUNDS, CHOSEN SEPARATELY FROM THE THEME.** Settings → Themes. A theme still hands you the world it was designed around, stored as an **empty id** so it keeps following later theme changes instead of freezing whichever backdrop was showing. Six new scenes, each a different *kind* of place rather than a recolour: Data Rain, Canyon (five parallax ridges), Aurora, Monolith (deliberately the still one), Deep Reef (the only scene lit from above), Dune. The picker draws each as a live thumbnail — they differ by structure, so a colour swatch would have shown ten identical squares.
> **⭐ THE GALAXY OPENS ON A STAR CHART.** Nine stars, six kinds, each drawn as itself. Touching one mixes its `branch` into every seed below it, so no two stars share structure at any depth and re-entering rebuilds the same universe. Pinching out of a dimension returns to the chart, not to Home. 37 `UniverseMathTest` cases now, including that the endless-zoom seam still holds *inside* a branch.
> **✨ THE ORB OPENS INTO THE GALAXY INSTEAD OF BEING REPLACED BY IT.** The transition was a cross-fade, which says *different screen* — the gesture says *you pulled that apart and went through it*, and none of the animation agreed. One `dive` progress now drives both layers: the app swells to ×1.6 and fades while the universe grows from 0.32, **both pivoting on the orb's measured position** (it sits above centre, so a screen-centre pivot makes the galaxy arrive from below it — two objects, not one becoming the other). The camera moves too: the dive starts *outside* the first shell at `ENTRY_ZOOM` and flies inward, so a structure rushes up to meet you rather than a picture settling. Entry bloom covers the frames where shells are too small to read; the readout waits until 55% in. 620ms in, 380ms out.
> **🪐 THE ORBIT ORB WAS DRAWN OUTSIDE ITS FRAME.** Widest ring 1.55 orb radii × an orb drawn at 0.86 of the half-frame = 1.33, and perspective magnifies the near side further as the tilts precess — worst case **1.46 half-frames against a drawing area of 1.0**, so it lost both edges on the device. Ring pulled in to 1.22 (worst case 1.09, so the orb keeps full size rather than being shrunk), and new `Orb3D.ringExtent` measures what a spec actually reaches by running the renderer's own projection over every precession phase, so `fitFor` can never let a theme clip again.
> **🔓 ONE `Color` IMPORT WAS HIDING EVERY ORB NUMBER FROM THE GATE.** `OrbStyle` lived in `JarvisPalette.kt`, so the specs keyed on it were behind jvmcheck's `ui/` exclusion. Enum moved to its own file, specs to `Orb3DSpecs.kt` — the whole orb geometry is now Compose-free and **OrbFitTest / OrbitThemeTest / Orb3DTest / OrbMathTest run off-device in milliseconds**. 51 test classes green before the push.
> **🎭 FOUR ORBS, NOT ONE ORB WITH FOUR RING COUNTS.** *"make the orb designs different across all themes, not only the background"* — accurate: every theme was tilted rings around a glow, so the accent was doing nearly all the work, the same fault the backdrops had. New `CoreKind` makes the centre the differentiator: **Arc** spark (untouched — "loved the arc reactor theme"), **Forge** molten mass with a crust, cracks and rising embers, **Nebula** diffuse with no surface anywhere plus gas lobes, **Orbit** a lit world with a terminator and limb.
> **🌌 GALAXY: gas, dust lanes, trails, and names.** Gas under everything (points on black read as a screensaver; the same points inside a cloud read as a place), dark lanes drawn with ordinary alpha because they are the one element that *takes* light away, satellite trails so a body is visibly travelling, and close-up bodies resolving into lit discs. Every structure now carries a seed-generated catalogue designation and a line saying what it is — which is also the visible proof the dive is reversible.
> **🩹 Two reported layout faults.** The home screen showed *through* a dive (both backdrop layers were translucent at 0.60/0.86 — now opaque), and the Settings "Themes" pill wrapped mid-word to "Theme / s" because a `Row` hands out width in order and the last child gets the remainder (equal thirds now).
> **🌌 THE ORB OPENS INTO A UNIVERSE YOU CAN FALL THROUGH** — from **every** theme. (The overlay first landed inside the `bottomDashboard` else-branch, which is the Orbit-only layout, so three of the four themes would have had an orb that did nothing.) *"i try and expand the orb like how u do with images on a screen… a galaxy kind of thing, i can keep going deeper and deeper"*. Pinch the home orb apart and it opens full screen; pinch again and you descend, endlessly. There is no ladder of authored levels to run out — one continuous `zoom` number, four **shells** on screen at once, and zooming in by exactly `1.0` grows every shell by `SCALE`, which puts the child precisely where its parent was. So the counter ticks over with nothing to seam. Content is hashed from *absolute* depth, so flying back up returns to the same system rather than a freshly rolled one. New pure `UniverseMath` (endless-zoom arithmetic, **17 tests**) + `OrbUniverse` (the renderer).
> **🔬 THE OFF-DEVICE GATE NOW REACHES INTO `ui/`, AND EARNED IT IMMEDIATELY.** `scripts/jvmcheck` excluded all of `ui/` because Compose cannot resolve here. But some files under `ui/` import no Compose at all — they are arithmetic that happens to serve the screen. Those are now compiled and tested here, and on the first run the new gate caught two real faults that would otherwise have shipped: a shell **0.1% visible on one side of every level boundary and absent on the other** (so the drawn set differed at each seam), and a **core left burning at 15% over the level below it**, which buries the very thing a dive exists to reveal. Both invisible in a diff; both obvious on a phone.
> **🎨 SEVEN THEMES CUT TO FOUR THAT ACTUALLY DIFFER.** Lattice, Prism and Core removed as asked. The cause of "they all look almost the same" was structural, not chromatic: in `ThemeBackdrop`, three pairs shared backdrop geometry, so three of seven were a neighbour recoloured. The four survivors get one signature element each and share nothing. **Orbit rebuilt** — four rings at 1.26/1.08/0.90/0.70 around a body of 0.55 is a scribble around a dot at thumbnail size; now three rings, the widest pushed to 1.55 so it plainly clears the body, core pulled back to 0.44.
> **🔍 FOUR BUGS FIXED FROM TWO DEVICE TRACES + THE PROVIDER DASHBOARD.** (1) `llama-3.3-70b-versatile` is **retired** — it led the SMART list, so every fresh process spent a 404 rediscovering that; removed from both tiers. (2) A **sixteen-second silence** mid-errand that the log could not explain, because every line in it reports an event that already happened and the transport allows 30s **per model**; the agent step now logs before it asks, reports its elapsed time, and is capped at 25s. (3) **"open YouTube" while already in YouTube relaunched it** — the "already in X — not relaunching" guard is real and tested but sits in `ScreenControlService`, and an Open-only plan took a shortcut branch straight to `AppLauncher`; now routed through the guard. (4) **JARVIS went deaf for the whole video** — the recogniser stands down for playback (correctly) and the background wake word was gated on there being no session, so *nothing* was listening for fourteen seconds; new pure `WorkSession.wantsHotword` hands it the mic in exactly that window, and a mid-session detection now claims the mic in place instead of yanking the app to the front.
> **🗣️ FIXED: "open Cloud" opened a Realme system app instead of Claude.** The user had already written `Cloud means Claude` into their instructions — sent to the model every turn — and the reply still said `<<OPEN|Cloud>>`, which is an *exact* label match for a real app on this phone, so the launcher opened it in good faith. New pure `control/AppAliases` parses the user's own "X means Y" rules out of instructions and learned facts and applies them **before** ranking, which is the only ordering that could work. Nothing pre-seeded — "Cloud" is a real app and may be the one someone else means. `X is Y` deliberately unsupported, so "my name is Manoj" cannot become an app redirect.
> **📜 FIXED: a long reply was unreachable.** `transcript`/`reply` were bare `Text` in a Column pinned to the viewport height, so anything past the fold was clipped with no way to scroll — the user got several paragraphs about backends and could read the first third. Both now scroll inside their own column, bounded as a *fraction* of the hero so the orb stays on screen on a small phone. Note the pairing: raising `max_tokens` to 900 yesterday made this worse, not better — a fix upstream exposed a bug downstream that had been invisible.
> **🗂️ FIXED: the Settings tab row sat BELOW the content it switched.** Three feature cards rendered unconditionally first, so their height was stolen from every tab beneath — on a phone the theme picker was one clipped row at the very bottom ("it doesn't give me space to scroll through themes at all"). They are now a **General** tab of their own, so each of General / Voice / Themes owns the screen. Blurbs trimmed from three sentences to one; each was wrapping to six lines. Also fixed: the "Settings" title was drawn under the host's menu icon.
> **🎨 THE THEMES REACHED ONE SCREEN IN SEVEN — fixed.** The user's "the themes are just not it" turned out to be countable, not taste: `HomeScreen` read `LocalPalette`; Settings, Instructions, Chat, Files, Calendar, Speech and Diagnostics all imported the fixed `Cyan` constant (13 references in Settings alone). Choosing Forge or Nebula recoloured the orb and left every other screen cyan. New `ui/theme/JarvisTheme` object of `@Composable` getters, all nine UI files swept onto it, and the glass fill on cards now takes the accent instead of a flat 8% white — which is why every card looked identical in all seven themes.
> **🌌 AND `ThemeBackdrop` NOW SITS BEHIND EVERY SCREEN, not just Home.** It was drawn inside `HomeContent`, so the app had one designed screen and six flat dark lists — open Settings and the world vanished. Moved to the host `Box`; also cheaper, since one instance means the starfield stays put while destinations change over it. **Mobbin was requested for references but needs a paid plan** (`Mobbin MCP requires a paid plan` on every query), so none were used.
> **⚠️ Compose is unverifiable off-device** (needs `dl.google.com`), so this rides on CI; `jvmcheck` is green but only proves no non-UI source broke.
> **✅ BACKEND PHASE 0 IS DONE.** `backend/` holds the proxy's quota and token accounting against a fake provider — **25 tests, 0 failures, zero dependencies**, plus a `backend` CI job that runs in seconds with no SDK and no emulator. The first part of this project ever verified *where it was written* instead of on a device twenty minutes later. Pinned behaviours: over the cap the provider is **never called**; two concurrent turns are both counted (the D1-not-KV reason); a provider failure charges nothing; and the by-one overshoot is pinned as intended, not a bug. **Auth is still a stub (`X-Uid`) — must not reach a public URL before Phase 3.** Next: Phase 1, blocked on a Cloudflare account.
> **🏗️ THE BACKEND PLAN IS WRITTEN — [`BACKEND_PLAN.md`](BACKEND_PLAN.md), all phases 0–6.** Phase 0 (the Worker against a fake provider, with quota + token accounting) is **startable with nothing from the user** and is fully testable here: Node 22 is present and a Worker-shaped module runs under `node --test` with **zero dependencies** — probe verified. Cloudflare unblocks Phase 1, Firebase unblocks Phase 3. **Phase 2 is the testing payoff** — firing every `SCREEN_CONTROL_EVAL.md` prompt at `/chat` and asserting marker shapes. Also corrected `COMMERCIALIZATION.md` §1d, which still named both now-retired llama models for the free/pro tiers.
> **☠️ FIXED: both llama models are now retired, and the fallback chain had run out.** `llama-3.1-8b-instant` went hours after `llama-3.3-70b-versatile`, leaving each tier with ONE live model — so a single "Empty reply from model" killed the turn twice. Both tiers now cross-cover on the two models this account's traffic proves alive, and an empty reply retries the same model once before falling through.
> **🧾 FIXED: "That's done." after typing nothing.** Asked to send Claude a prompt, the loop tapped once and declared success. Done is now refused when the goal names composing something and no `Type` step ever ran — nudged, not abandoned.
> **✅ Confirmed working in the same trace:** the "Cloud means Claude" alias, the floating orb, tap-to-interrupt, and the new `asking for the next move` / `answered in 1296ms` timing lines.
> **🛑 FIXED — the worst bug of the session: "do it" sent a message JARVIS had rewritten.** Asked to confirm a Send, the answer went back to the *model*, which invented a different message, typed it and sent it. `AgentMove.Ask` carried only the question and threw the step away. It now carries the exact step, and a plain yes runs **that step alone** — no model round trip, so nothing new can be invented. New pure `Confirmation` matches whole utterances only ("yes but change the wording" is deliberately NOT a yes).
> **🎙️ FIXED: you could not interrupt a running task.** An errand is mostly silence — a step executing, a model call in flight — and the turn is mic-gated throughout, with `BARGE_IN` conditioned on speaking alone. New `WorkSession.busyWithTask` keeps the mic reachable, and `interrupt()` now stops the **task** (bumps the token, cancels the sequence, idles the turn) rather than just the sentence.
> **🗣️ FIXED: it read the asterisks out loud.** The model formats for a screen and TTS says every character — "asterisk asterisk U I testing". New pure `voice/SpokenText` cleans formatting on the way to the **speaker only**; the chat screen keeps its bold and bullets, because that is what makes a long answer readable. Applied at `speakTurn`, the single path to the speaker. 13 tests.
> **✂️ FIXED: long replies stopped mid-word.** `max_tokens` was 300 (~220 words) — an answer about Android testing ended `* **UI`. Now 900.
> **🗑️ REMOVED: the "JARVIS is controlling the screen" tint**, entirely — view, window, safety timer and all four call sites.
> **🔵 The orb got a dismiss and a new design.** Drag it onto the ✕ that appears at the bottom (discovered by dragging, cannot misfire like a long-press) and it turns the setting off. Redrawn as **waves and nothing else** — orbits, dust and core all removed. The swell is frozen unless somebody is actually talking (thinking included: it stays still and is told apart by colour), and the colour says *who* — accent for JARVIS, the warm highlight for you with the swell driven by the real mic level.
> **🔁 FIXED (reported on device): the orb outlived "thank you Jarvis".** The rule was right, its *placement* was wrong — decided only in `resume()`/`pause()`, and the stop phrase ends a session from the background without either firing. **Exactly the wake-word gap again, three commits after writing the note about it.** `applyBubble()` now sits beside `applyHotword()` in `applyMicOwner`, where every session change passes; the rule is a pure `WorkSession.wantsBubble` with six tests.
> **🎨 The orb was redrawn to the user's brief — no rings outside, waves inside.** Three stacked sine bands clipped to the disc, like liquid with a swell in it; the swell height *is* the state (tall/quick speaking, real mic level listening, slow roll thinking, nearly flat idle). Body is a vertical ramp lit from below rather than a centred radial, which is what stopped it looking like a button.
> **🔵 NEW: a floating JARVIS orb over other apps, with NO new permission.** `control/OrbBubble` — draggable, snaps to the nearer edge, remembers where it was put, shows what JARVIS is doing, and tap means what the in-app orb means (cut him off if talking, otherwise listen). It borrows `TYPE_ACCESSIBILITY_OVERLAY`, which accepts touches — no `SYSTEM_ALERT_WINDOW`, no settings trip, nothing extra to disclose to Play. Drawn by a `View` on a `Canvas` rather than Compose, so the off-device gate can still compile it; `BubbleColors` is a guarded plain-int copy of the palette. The full-screen control tint now stands down while the orb is up. Settings toggle, default on.
> **🎨 The home backdrop was rebuilt — it was one flat wash on a 150-second clock nobody could perceive.** Now a vertical depth ramp, four additive aurora blooms on a 38-second clock so overlaps genuinely brighten, a horizon glow, and a vignette last so the brightness stays affordable. Starfield roughly doubled, brighter, twinkling on per-star phases. Placement stays deterministic — a Canvas redraws every frame, and a real random re-scatters the sky into static.
> **👁️ AND A FIFTH: he could see the screen all along.** A trace has him answering "can you read my screen" five times with "I don't have visual access to your screen" — while the accessibility service was connected and `buildContext` was feeding `describeScreen()` into his context on every turn, the same listing the executor taps from. One prompt clause ("that listing is for YOU — never read it out or describe it back") was generalised by the model from *do not describe it* into *cannot see it*. Now: never narrate it unasked, but answer when asked, and never claim blindness while holding it. `buildContext` also logs what it read (size + app line, never the contents), because nothing in the trace could previously say whether the listing had reached the model at all.
> **✅ CONFIRMED ON DEVICE: voice barge-in works** (`barge-in — heard "Hey Jarvis" over the reply`), and his own TTS does **not** mask the wake word — the tuning I expected to need is not needed. Natural replies confirmed too.
> **📊 The real constraint is tokens, not requests:** the Groq dashboard shows `gpt-oss-120b` hitting its **8.3K tokens/minute** ceiling while requests stay far under 30/min. Worth knowing before Part E's metering design.
> **🧰 `scripts/jvmcheck/` is committed** — the off-device gate, finally a file instead of a recipe: `gradle -p scripts/jvmcheck test` → **432 tests, 0 failures** in ~10s, type-checking all 50 non-UI sources against a real `android.jar`. Run it before every push.
> **🔌 E2E probe — two bugs stacked, and the first was hiding the second (CI pending).** My earlier `kotlin-stdlib` "fix" addressed neither. **Bug 1: the click handler could not run.** Kotlin emits calls into `kotlin.jvm.internal.Intrinsics` for its null checks, and the fixture's process has only the test APK on its dex path. `androidTestImplementation("kotlin-stdlib")` was accepted, resolved and **packaged nowhere** — **AGP strips from the test APK any dependency already present in the app APK**, which is normally right because an instrumentation test shares the app's classloader, and wrong here because the fixture is a separate process. Declaring a dependency is a *request*, not a guarantee. `FixtureActivity` is now **Java**, which emits no such calls, so the dependency stops existing rather than needing to be packaged. **Bug 2, the one that mattered: the test and the fixture were never in the same process.** The fixture recorded taps in a `@Volatile` static and the test read it back — impossible, and the crash was masking why. The instrumentation runs in `com.jarvis.os` (it reads `ScreenControlService.instance`, a static that only exists there, and got a non-null answer); the crash logs the fixture in `com.jarvis.os.test`. **A static written in one is invisible in the other**, so the Java port alone would have produced `expected:<Add to cart> but was:<null>` — the same message the probe has printed all along, with nothing left in the log to explain it. A crash silently converted into a wrong answer is worse than the crash. The tap is now recorded **in the fixture's own visible text and read back through the accessibility tree**, which crosses processes by design and is a stronger claim than the field ever was: it proves the tap landed *and* that the result is observable through the very mechanism JARVIS uses to read a screen. The failure message now separates the three outcomes — status still idle (tap never arrived), decoy named (wrong control), target named (pass). **Verified as far as this environment allows:** the Java fixture compiles against the real Android 16 framework and `javap` shows **zero `kotlin` references and zero `invokedynamic`** in its bytecode — the exact property the fix depends on, checked rather than assumed. `String.concat` rather than `+`, because at source 17 `+` lowers to an `invokedynamic` against `StringConcatFactory` that D8 must desugar, and this file exists because a packaging assumption about the test APK was wrong twice.
> **🗣️ He no longer says "On it." to everything (CI pending).** Every reply that carried an action was the same two syllables — open Spotify, search Amazon Music, add bread to a basket, all "On it." — and the same went for "Yes?" opening every session and "Anytime." closing it. **The wording was the symptom, not the problem.** Those lines only fire when the *model said nothing*: the prompt already asks for one short natural sentence before the markers, and when one arrives it is spoken verbatim, as it always was. What the app did badly was fill that silence identically every time. New pure `Acknowledgement` makes the floor worth hearing: it **says what is actually happening**, derived from the plan so it cannot name the wrong app ("Opening Spotify" is no longer than "On it" and tells the user something), and a search **repeats the query back** — "Looking for my pic playlist in Amazon Music" — which doubles as the cheapest confirmation it heard the words right, in an app that mishears "pic" as "peak" routinely. Nothing repeats twice running; a turn **counter** rotates the wording, not a random draw, so the object stays pure and its tests pin exact strings. Two notes that were bolted onto otherwise-fine replies are much shorter — a good sentence followed by a paragraph of hedging is still a bad reply: SpendGuard's became *"I've stopped short of Checkout — say the word and I'll do it"*, and the alarm-volume note *"Your alarm volume is off though, so it won't make a sound. Want me to turn it up?"* **Deliberately unchanged: `SendGuard`, `SpendGuard` and `AlarmGuard` still drop the actions they always dropped** — none of them alters a word JARVIS says, they only subtract steps (the unasked send, `Tap(Checkout)` on "add some bread", the timer called "nap" from "play Beat It"). Removing them would not make the chat natural, only let him send messages and buy things unasked — Rule 6, and three device traces. One test was **loosened rather than satisfied**: `SpendGuardTest` required the literal words "Tell me", which pins phrasing rather than behaviour; it now checks the reply names the withheld step and offers a way to authorise it. 419 tests, 0 failures.
> **🎙️ Barge-in complete — tap OR voice (part two, CI pending).** Part one made interruption possible at all; this makes it reachable when it matters. Tapping the orb only works while JARVIS is on screen, and the moment you most need to cut in is mid-errand from *another* app, where there is no orb. Say **"Hey Jarvis" over him** and he stops and listens. **Why the wake word and not a voice-activity detector:** the speaker sits centimetres from the microphone, so his own voice arrives 20–30 dB above the user's, and platform echo cancellation does not apply — it references the *communication* downlink while TTS plays on `STREAM_MUSIC`, so `AcousticEchoCanceler` reports itself enabled and cancels nothing. An energy VAD there does not detect barge-in, it detects JARVIS, every time: speak → hear self → stop → silence → sleep. Worse than shipping nothing, and undiscoverable until it is on a device. **"Hey Jarvis" is immune by construction — JARVIS never says it.** Reuses `OpenWakeWord` on the same `AudioRecord` loop `HotwordService` already runs (`VOICE_RECOGNITION`, not `VOICE_COMMUNICATION` — the latter brings its own AEC but changes the audio route and gain, putting the models off-distribution for a benefit this stream never gets). **The listener is an OWNER, not a second listener:** `MicOwner.BARGE_IN`, above everything but the permission check, so "two mic owners" stays unrepresentable — the exact condition that forced the 2026-07-26 revert. Two rules ship with it, both tested: **speaking while backgrounded with no work session yields `NONE`** (Android 9+ hands a background app with no microphone FGS **silence, not an error** — a listener there reads zeros forever and looks, in a trace, exactly like a user who said nothing), and **speaking beats playing media** (the yield-to-media rule must not fire against his own voice). `applyMicOwner`'s early return now lets `BARGE_IN` through, since that owner exists *because* the turn has the mic gated; speaking state is **derived** there from the turn phase rather than announced by each speak path, so the two cannot drift. **Two races caught in review, neither testable here:** (1) on detection the loop cleared its `running` flag, so the following `stop()` reported "nothing was recording", the caller skipped the hand-off gap, and the recogniser opened the input while the audio thread was still inside `recorder.release()` — it now `break`s and leaves the flag set so `stop()` *joins*; (2) the model load runs on the audio thread and takes long enough that a short reply can finish inside it, so a `stop()` in that window returned while the thread went on to seize the microphone anyway — it now re-checks after the load and after constructing the `AudioRecord`. Hand-off gets a deliberate **`MIC_HANDOFF_MS = 200`**: the `AudioRecord` is released but the audio HAL does not always free the input immediately, and `SpeechRecognizer` answers a lost race with `ERROR_RECOGNIZER_BUSY`, which surfaces as JARVIS simply not hearing the thing you interrupted him to say. **Expected to need device tuning:** his own TTS may *mask* the wake word at that distance so the detector never crosses threshold — the lever is lowering TTS volume while armed, left alone until there is a trace to tune against. 408 tests, 0 failures off-device before pushing.
> **✅ Barge-in part one CONFIRMED ON DEVICE (15:41–15:43 trace).** Four interruptions, four clean recoveries — `interrupted — listening` each time, followed by the next command being heard. No watchdog line anywhere, so no turn ever stuck. **Still unexercised:** every one of those four interrupts landed on a reply with `screen=none`, so `pendingScreen = null` — the part that stops a queued app opening after you cut him off — has *not* been tested on a device.
> **✅ MERGED to `main`. Eight bugs fixed from the 2026-08-14 device trace:** the session's root cause was that **the errand loop decided its first move ~1s after launching an app, before the app had drawn** — the trace logs `choosing … from 1 on-screen options` one second after `Open(YouTube)`, whose feed has dozens. The model was handed a splash screen and answered the only ways it could: a reflexive `<<BACK>>` or a `<<PICK>>` against a list of one. Fixes: (1) **wait for the app to render** — new pure `AgentLoop.looksUnrendered()` (blank / remembered / under `READY_ITEMS` interactive items), re-looked up to `MAX_RENDER_WAITS` times, and **only before the first action** (later a sparse screen is real information). (2) **A first-move `<<BACK>>` no longer ends the errand** — `JUST_ARRIVED` joins `GOING_IN_CIRCLES` in `AgentLoop.NUDGEABLE`; three errands in a row died on their opening move. `ALREADY_FAILED`/`LEFT_APP` stay non-nudgeable (they name a route already known wrong). (3) **A failed launch says so** — `Open(app=Search)` failed honestly and the loop drove on anyway; now `AgentLoop.couldNotOpenMessage()` instead of blaming the screen. (4) **`say()` now sets `busy`** — TTS rides the music stream, so the media check heard JARVIS talking and yielded the mic after *every* agent-loop line (`audio started — pausing listening` 2s after each), leaving the user unable to answer the question just asked; `ask()`/`speakAck()` always set it, `say()` never did. (5) **A marker that was a sentence's subject no longer leaves a headless reply** — `<<TAP|Add to wishlist>> isn't the right control…` was spoken starting at "isn't"; guarded to replies that *begin* with a marker, bounded to the first sentence end. (6) **Stripped marker chains no longer leave a hole** (one reply showed six blank lines). (7) **The playbook stops learning and replaying complaints** — `"you didn't do anything"` was stored as a route, replayed, and **re-learned itself** (2 uses in one session); `Playbook.isComplaint()` blocks both halves and `match()` refuses even a poisoned entry already on the phone; a replayed route is also no longer written back or re-driven as an errand. (8) **Search-box chrome kept from the chooser** — asked to pick "how it works" it answered `Clear`. Plus a prompt rule: **ask only what you need to act now, never for details JARVIS cannot set** (delivery slots, addresses, payment) — five turns of the session were lost to exactly those, with `AskGuard` correctly dropping the plan each time so nothing happened. Paid for by moving two pieces of pure rationale into the KDoc; **5,914 chars, under the tested ceiling**. 24 frozen regressions in `DeviceTrace0814Test`.
> **Latent bug found while reviewing the above:** the **playbook-replay path never reset `agentSteps`/`stepsTaken`**. A replay still runs through the service with recovery enabled, and recovery consults the agent budget — so a budget left over from a previous errand could refuse to recover a replayed route before it had taken a single step. The model path always reset both; the replay path never did.
> **🧠 Learning layer, first half shipped — `control/ControlVocabulary` (CI pending).** The Blinkit failure in one line: the plan aimed at `Tap("Search")` and the app's search control is labelled **"Search for atta, dal, coke and more"** — the plan was not wrong, the string could not have been known before the app was open. `ControlVocabulary` translates the generic word the model reaches for into the literal label a given app uses, keyed by package **fragment** (Blinkit ships as `com.grofers.customerapp`, so an exact key would miss it and every regional variant besides). Seeded from the traces: Blinkit, Zepto, YouTube, Amazon, Swiggy, Zomato. **Wired into `seek()` strictly additively** — it runs only after the requested label has already failed to match confidently, only acts on a match ≥ `GOOD_SCORE`, and otherwise leaves the scroll-and-weak-match path untouched. So it can add a way to succeed and cannot remove one, and a stale seed fails like any other wrong label rather than breaking the step. Instagram/WhatsApp are deliberately **not** seeded — their controls are "Search"/"Search…", already matched at 90 by the existing prefix scoring, so a seed would be a dead row implying coverage that does not exist. 9 tests, expectations pre-flighted. **Still to come:** the *learned* half (record literals from runs that ran clean, on the same `ok && clean` gate `Playbook` uses) and `AppRegistry` for spoken-name → app.
> **🔧 Four device-reported faults in the Orbit theme, fixed (CI pending).** From screenshots: (1) **the dashboard bar was drawing over every screen** — on Settings it landed on top of the theme picker and read as a rendering fault. It is Home-only now; every other destination has its own scrolling layout that owns the bottom of the screen, and a floating bar over it is debris, not navigation. (2) **A right-swipe still opened the side drawer** — the theme had replaced it, but `ModalNavigationDrawer`'s edge gesture was still live, so the drawer was there, just invisible. `gesturesEnabled = !bottomDashboard`. (3) **Settings lagged badly** — the picker was running **seven** live orbs, each an infinite transition invalidating a Canvas at 60fps. Only the selected card animates now; `HudOrb` gained an `animated` flag that skips creating the transition altogether (gating the values would have saved nothing, since an unused transition still schedules frames). Trade named honestly: three themes differ mainly in how they *move*, so a still card shows less — tap it and it moves. (4) **The orb looked nothing like the reference** — `coreSize` was 0.34 and the screenshot showed the truth: a small dim blob with thin ellipses dominating, i.e. the rings had become the subject. The reference is a *sphere* with rings around it, so the body is now 0.55, far larger than any other theme (next is 0.19). **New gesture:** with the bar gone from other screens, a **pull down from the top** returns Home — confined to a 72dp strip so it cannot fight the scrolling below, and with a generous threshold because it competes with the notification shade.
> **🧭 Orbit navigates from a bottom dashboard, not the drawer (CI pending).** Per the design, the top-left menu button is **gone in this theme** — the top of the screen is the greeting and the bottom already carries the bar, so a floating icon over the planet horizon read as a mistake. Tapping the bar (chevron over "Tap to open dashboard" / "ALL FEATURES, ALL IN ONE PLACE") slides up a scrollable panel of every destination, capped at 78% height so the orb stays visible behind it — the sheet reads as something drawn *over* the assistant rather than a different screen, which is the point of moving navigation down there. Scrim tap and Back both close it. **Theme-scoped:** the other six keep the drawer, so this is a property of the look rather than a change everyone has to accept. The bar shows on every destination, not just Home, because in this theme it is the only way to navigate — hiding it elsewhere would strand the user with nothing but Back. Back handling is an explicit if/else chain rather than two independent `BackHandler`s, since with both registered the winner depends on composition order.
> **🌍 Seventh theme — "Orbit", from the 2026-08-16 mockup (CI pending).** A lit world under wide orbital rings, over a planet horizon. Built on the existing procedural 3D system rather than as artwork, so it moves. What is genuinely new: **`planetHorizon`** in `ThemeBackdrop` — a circle far larger than the screen whose top edge crosses the lower third, giving the shallow curve of a horizon seen from orbit, with a lit atmosphere hairline and deterministic city lights on the dark side (`OrbMath.unitRandom`, never `Math.random` — a Canvas redraws every frame and a re-rolled scatter renders as static, not a city). Palette pairs **cyan with violet** rather than the cyan/blue every other blue theme uses, because the reference's sphere runs cyan at the rim and violet on the far side. Darkest ground of the seven (`#03070F`) — the design is mostly empty space with one bright object in it. **Motion is the point and is now tested:** four rings, speeds following real orbital mechanics (inner fastest, widest slowest), alternating directions so crossings are head-on, no two speeds equal, and arcs ~0.5 so the travelling highlight has room to be seen. **A first draft got this wrong** — arcs at 0.80–0.92 lit the rings end to end and made it the *least* animated theme of the seven, in the one design that is entirely about movement; `OrbitThemeTest` now pins all of it, including that the other six still move. Also caught pre-push: the colour ramp originally fell to near-black at its outer stops (that is `backdrop`'s job, not the ramp's), which would have made the widest, most characteristic rings almost invisible.
> **🎉 E2E tap tier — THE TAP LANDED. All three unknowns answered; one packaging fix left.** Run #255's logcat is the proof the whole tier was built for:
> ```
> at com.jarvis.os.e2e.FixtureActivity.onCreate$lambda$2$lambda$1(FixtureActivity.kt:50)
> at android.view.View.performClick(View.java:7659)
> at android.widget.TextView.performAccessibilityActionClick(...)
> at android.view.AccessibilityInteractionController.performAccessibilityActionUiThread(...)
> ```
> The accessibility service found the node, performed the click, and **the fixture's own `onClick` handler ran** (line 50 = the "Add to cart" callback). Service binds → fixture in front → `runSteps(Tap(…))` → real tap → fixture reacts. The approach is **proven viable**; the `:fixtures` module fallback is not needed.
> It failed only *after* that, when the recorder crashed: `NoClassDefFoundError: kotlin.jvm.internal.Intrinsics`, missing from `com.jarvis.os.test/base.apk`. The fixture runs in the **test process**, whose dex path is the test APK alone, and Kotlin emits `Intrinsics` calls for its null checks. Fixed by packaging `kotlin-stdlib` into `androidTestImplementation`. **A packaging gap, not a behavioural one — the tap was never the problem.**
> **Previously: assumptions #1 and #2 SETTLED.** Run #251: the fixture launched and `rootInActiveWindow` was readable, so **an androidTest-APK Activity does present a package the executor will act on**. The assertion still failed — reading `com.android.launcher3` — because my poll waited for a *non-null* package rather than the *fixture's* package, and `rootInActiveWindow` is non-null immediately: it was still the launcher. **That is the exact bug class this tier exists to catch, committed by the test meant to catch it** — an app is in front long before it is the app you asked for. Now waits for the fixture specifically. Cycle-by-cycle the failure has moved strictly forward: service won't bind → binds, activity not found → activity found, wrong app in front → the tap.
> **Previously: assumption #1 SETTLED — the accessibility service DOES bind from a test.** Run #249 moved the failure past it — the `UiAutomation` suppression fix worked, and *"can a real `AccessibilityService` be enabled and bound from an instrumented test?"* is now answered **yes**. The run then died later and smaller: `ActivityNotFoundException: {com.jarvis.os/…FixtureActivity}` — the Intent was built from `targetContext`, aiming an explicit component at the app package, while the fixture lives in the androidTest APK (`com.jarvis.os.test`). Fixed by building it from the **test** context. That error is itself the first hard evidence the two packages are genuinely distinct, which is exactly what assumption #2 requires.
> **Root cause of the original bind failure (fixed, `ba056e2`).** The probe's own diagnostics settled it in one cycle: `enabled_accessibility_services` **stuck**, `accessibility_enabled = 1`, `AccessibilityManager.isEnabled = true`, our service **installed** — and `enabled services = (none)`. Nothing had failed; something had actively switched it off. That something is **`UiAutomation`: connecting it suppresses every other accessibility service by default**, and the probe's own `shell()` helper was what connected it — *the instrumentation used to enable the service was disabling it*. Fixed by taking the connection with **`FLAG_DONT_SUPPRESS_ACCESSIBILITY_SERVICES`** via a single accessor used by every shell call (the flag applies only as the connection is created, so one unflagged call anywhere reintroduces the suppression). **The app was never at fault** — no manifest, permission or timeout change was needed, and none would have helped.
> **Previously (same tier):** the probe RAN AND FAILED — which is the point of a probe. `A11yProbeTest` reported: *"the accessibility service never bound"*. Writing `enabled_accessibility_services` by shell did **not** get the service bound within 15s on an API 34 `default` emulator image, so the tier's foundational assumption does not hold as written. Everything downstream — the fixture-package check and the actual tap — never ran, so **those two questions are still open**. One test and ~4 minutes bought that, instead of a ten-test suite built on an assumption that is false. **Cause NOT yet diagnosed, and deliberately not guessed at.** Two reporting defects fixed first (`2953815`): the probe's failure now carries whether the settings write stuck, what `AccessibilityManager` lists as enabled/installed, and the test/target processes — enough to tell "the shell write failed" from "the write worked and the system still refused to bind", which need different fixes; and the bind window went 15s → 40s (the old value was a guess).
> **⚠️ GOTCHA banked — `continue-on-error` on a STEP makes CI unreadable.** It was set on the probe step *and* its job, so the jobs API reported the step's conclusion as `success` while the test had failed, and all four jobs looked green. The failure was only visible by reading the raw log. It now lives on the **job only**: still non-gating, but the step tells the truth. Same shape as "in progress is not evidence of progress" — using the signal the API offers first instead of the one that is true.
> **🔬 E2E tap tier — probe design (merged to the branch).** The pyramid had **no test anywhere that drives a tap** — the two instrumented tests load a model and render one Compose screen — which is exactly why the 2026-08-14 integration bugs could not be caught before a device. `A11yProbeTest` is deliberately **one test**, settling the two unknowns the whole tier rests on: whether the accessibility service can be enabled from a test at all (shell-writing `enabled_accessibility_services`), and whether an androidTest-APK Activity presents a package the executor will act on — `awaitApp`/`userAppRoot` both refuse JARVIS's own package, so if the fixture reads as `com.jarvis.os` the approach is dead and the fallback is a separate `:fixtures` module. `FixtureActivity` is **plain Android views, no Compose, no animation** (the emulator VM was reliably taken down by `JarvisAppUiTest`'s animated `HudOrb`). It asserts the **fixture's own click callback fired**, not that the executor reported success — trusting the latter would only re-test the bookkeeping that once announced "Playing the Thriller video" four times while doing nothing. Runs in its **own `continue-on-error` job**, and is **excluded** from the proven-green `instrumented` job by `notClass`, so the documented ~8-test capacity ceiling is not disturbed. No new dependencies.
> **Still open from the same trace (NOT fixed, and the user asked for it):** the agent **cannot deliberately scroll** — "could you scroll down into my playlist" made the loop thrash `Tap(Peak) → Tap(Find) → Tap(View Library) → Tap(Peak) → …` until the repeat guard stopped it. There is no scroll verb in the agent vocabulary; `<<TAP>>` only scrolls *while hunting a label*. This is the next feature, not a bug fix.
>
> Previous snapshot: `main` @ `b1158b6` · build **#237** · 2026-08-09
> **⏭️ NEXT UP (designed, not started — see [`SESSION_HANDOFF.md`](SESSION_HANDOFF.md) "NEXT UP"):** an automated **E2E tap-testing tier** (enable the real a11y service on the emulator + fake-app fixtures — the pyramid currently has NO test that drives a tap), a **learning layer** (pre-seeded + learned `AppRegistry` + per-app `ControlVocabulary`, generalising `Playbook`), and **100 more accuracy scenarios**. Open decision: build the E2E harness first (recommended) or the learning feature first (user's stated preference).
> **Device trace → five bugs fixed in code (MERGED to `main`, build #237 green):** an on-device eval surfaced real failures the automated suite missed, split honestly into three causes. (1) **Alarm confirmed across turns was dropped** — "set an alarm for tomorrow" → "6 o'clock" suppressed the alarm because `AlarmGuard` only saw the latest utterance; it now takes the prior assistant turn, so a bare time answer confirms an alarm JARVIS just asked the time for (negation still wins). (2) **Phantom `<<BACK>>`/`<<HOME>>` backed out of a just-opened app** — `AgentLoop.parseMove` now refuses a Back/Home before any in-app action (nothing to go back from right after an Open); prompt updated. (3) **Recovery typed the user's conversational sentence into a field** — `parseMove` refuses a `<<TYPE>>` echoing the whole sentence-length goal; prompt says type only the query terms. (4) **`<<OPEN|non-app>>` silently "succeeded"** — `ScreenControlService`'s Open branch now `failed()`s on a null resolution like every other step. (5) **The PICK chooser picked the "Reels" nav tab** — new pure `control/PickFilter` drops nav-chrome labels before the chooser (never emptying the list); `CHOOSER_PROMPT` names reel/story/short/post. Frozen regression tests for all five; `docs/SCREEN_CONTROL_EVAL.md` §F lists the on-device re-verify. **Why the 50-scenario suite missed them:** two were deterministic bugs it never exercised (single-utterance alarm tests; no Open→BACK scenario + no guard), one was a silent executor gap with no test, and two are model-layer (chooser judgment, typed-goal) that the suite documented as device-only — now guard-railed + prompt-hardened + device-eval'd, not left to the model alone. Harness note: every errand test already feeds the leading `<<OPEN|app>>` as its first reply, so `taken` mirrors the engine's seeded `errandSteps` — the guard fires on it; no naive re-seed (it would double the Open and break the count assertions).
> **Test pyramid A2 shipped (merged to `main`):** the "tap the right control" scoring + credential redaction moved into a pure `control/ScreenMatch` object, and the Groq/Gemini JSON parsers opened to `internal`, all now covered by fast tests (`ScreenMatchTest` pure-JVM; `Groq/GeminiClientParseTest` Robolectric). The device-only "tap the wrong thing" class and the pre-send redaction are now locked in CI.
> **Screen-control accuracy: 50 behavioral scenarios + 2 real guard bugs FIXED (CI pending):** new `ScreenControlAccuracyScenariosTest` (music/shopping/messaging/nav/safety) asserts the post-guard executed plan for 50 realistic prompts. It surfaced two genuine defects, both fixed: (1) **negation defeated the guards** — "don't check out" / "don't send" / "don't set an alarm" flipped the safety guard OFF; new pure `assistant/Negation.kt` makes matching negation-aware. (2) **`SpendGuard` silently broke messaging** — it truncated at ANY irreversible tap incl. "send"/"call"/"share"/"delete", so "send mom a message" had its `Tap(Send)` stripped and never sent; rewritten to **per-action authorization** (withhold an irreversible tap only when the user didn't name that action). Companion on-device checklist `docs/SCREEN_CONTROL_EVAL.md`. All tests pure JVM (fast `build` job). Honest boundary: does not test the LLM's plan or real-app taps (device-only).
> **Rigorous Parts A/B/C/F scenario suite (~50 tests, green in `build`):** `ScreenControlScenarioTest` (Part A multi-step + Part C long errands via the guard chain + `AgentLoop.parseMove` driver), `ScreenMatchAccuracyTest` (Part C "tap the right control" + redaction), `WorkSessionScenarioTest` (Part B one long mic-owner lifecycle + phrase tables), `AlarmGuardScenarioTest` (phantom-alarm suppression), and Part F (`ArtifactStoreRobolectricTest` + `ArtifactActionsScenarioTest`). All pass in the fast `build` job. Honest scope: Part F's flow-chart/diagram + image generation are NOT built (not testable); its text/PDF **logic** is covered, but on-device `PdfDocument` rendering is a Rule-5 device-only check (an emulator test for it was tried and pulled — it reliably crashed the GitHub emulator VM under the fuller instrumented load, runs #222–#225; the crash was infra, never a test assertion, and the emulator job is back to its proven-green 8-test set). Device-only ceiling unchanged: real taps on real app layouts, the LLM's PICK choice, real PDF rendering.
> **Test pyramid COMPLETE (Compose UI tier added, CI pending):** added the emulator Compose-UI tests — `InstructionsScreenUiTest` (append example → Save; Forget→onForget). (`JarvisAppUiTest` was removed — rendering `JarvisApp`'s animated HudOrb reliably crashed the emulator VM). The screen-control a11y-binding emulator test was deliberately NOT built — it needs the real bound AccessibilityService (flaky, shell-enabled) with no correctness value beyond `ScreenMatchTest`; that boundary is the documented device-only ceiling. **All six pyramid layers now live.**
> **Wake-word load — CAUGHT in CI then FIXED by reimplementing the melspectrogram in Kotlin (no device); now MERGED to `main`:** the emulator instrumented job caught the `CONV_2D ... BytesRequired overflowed` load crash on **every** Android runtime tried — `tensorflow-lite` 2.16.1/2.17.0 (frozen; 2.17.0 is Maven Central's last) **and** LiteRT 1.0.0/2.1.0 — while the Python `ai-edge-litert` runtime loads the same model fine. The bug is Android-runtime shape-inference on the melspectrogram model's **dynamic-length input**, not fixable by a version bump. Fix: the melspectrogram (a standard librosa power-to-db mel) is now computed in **pure Kotlin** (`voice/MelSpectrogram.kt`) using the model's **own extracted weights** (`assets/openwakeword/melspec_weights.bin`, generated by `scripts/owwtest/extract_melspec_weights.py`); the two fixed-shape feature models stay on the standard `tensorflow-lite:2.17.0`. Verified in Python (matches the model to ~1.5e-5; identical end-to-end detection 0.998/0.000) before pushing, then **CI-green on the emulator (run #211): load + detect + silence all pass on a real Android runtime.** `OpenWakeWord.lastLoadError` names any future load failure's real cause in CI.
> **This branch (build pending CI, not device-confirmed):** speech now keeps the recogniser's n-best alternatives and hands the near misses to the model to recover a mis-heard word ("peak" heard as "pic"); the custom-instructions screen was rebuilt; a translucent "JARVIS is controlling the screen" tint shows while it drives the screen (accessibility overlay, no new permission); and the **"Hey JARVIS" wake word is back** (in-app asleep/awake, rebuilt to avoid the two 2026-07-26 failures) with tap-to-wake on the orb. See the first sections below and the 2026-08-06 memory entries.
> **Everything is merged — branch and `main` are level, nothing red, nothing unmerged.**
> **⚠️ Screen control does NOT work on a device yet.** Two sessions (Blinkit 2026-08-04, Zepto 2026-08-05) both ended with the errand unfinished, and the second was a regression: 18 useless taps per command. That is bounded now (repeat guard, stall guard, cancellation token, budget 8) but **nothing about the loop is device-confirmed**. See [`SESSION_HANDOFF.md`](SESSION_HANDOFF.md) — read it before building on the loop.
> **Next up: the backend.** Full implementation brief in [`COMMERCIALIZATION.md` §1d](COMMERCIALIZATION.md) — Cloudflare Worker + D1, Firebase Auth, per-user *token* metering. Start with the Worker: it is plain JS and the first part of this project that can be genuinely tested inside a Claude session.
> **✅ MERGED to `main` (`3e767fa`), artifact confirmed.** Newest: **barge-in, part one — JARVIS can now be cut off mid-sentence.** Tapping the orb while he speaks stops him *and starts listening*. The button was never the hard part; three things had to be fixed before one could work. **(1) `speaker.stop()` deadlocked the loop.** `Speaker`'s `UtteranceProgressListener` overrode `onStart`/`onDone`/`onError` but **not `onStop`** — and Android delivers `onStop`, not `onDone`, for an utterance cut short. Stopping mid-sentence therefore produced *no callback at all*: `onSpokenDone` never ran, `busy` stayed set, and `applyMicOwner()` returned early for the rest of the session. The first interruption would have been the last thing JARVIS ever heard. **(2) `QUEUE_FLUSH` makes "speech ended" ambiguous.** `speak()` flushes, so a second reply cuts off the first and the platform then reports the **first** as ended, *after* the second has started. Adding `onStop` without telling utterances apart would fire "the reply is over" for a turn already gone — which opens the mic, which `muteEarcon`s `STREAM_MUSIC`, and TTS plays on `STREAM_MUSIC`; JARVIS would have gone silent halfway through his own answer. Every utterance now carries a monotonic sequence and anything stale is discarded. **(3) `busy` was one boolean doing three unrelated jobs** — mic gate, "the sound on the speaker is JARVIS not your music" for `mediaCheck`, and holding off the sleep timer. Barge-in needs the first cleared while the other two stay set, which one flag cannot express. Now pure `TurnState` (`micGated` / `ownVoiceOnStream` / `suppressSleep`), **14 tests**, in the manner of `WorkSession`. Interruption is **synchronous** — `tts.stop()` on an idle engine produces no callback ever, so anything awaiting confirmation waits forever; `pause()` idles the turn the same way. `interrupt()` also clears `pendingScreen`, or cutting him off would look like it worked and then launch an app anyway. Plus a watchdog on **its own `Handler`** (`ask()`/`pause()` call `main.removeCallbacksAndMessages(null)` and would silently bin one posted on `main`), and a latent fix: **`restartSoon()` reached `voice.startListening()` directly, bypassing `applyMicOwner`**, while the KDoc two functions above claimed no such path existed.
> **🔬 Off-device verification is now possible, and this change used it.** Maven Central is reachable from the build container; it is **Google's** Maven (`dl.google.com`) that the network policy 403s. Since the 50 non-UI sources reference nothing in `ui/` or `R`, they type-check against the real Android 16 framework (`org.robolectric:android-all`) and the 45 pure-JVM test classes compile and run off-device: **402 tests, 0 failures** before pushing. The Compose layer still cannot be built here. See `SESSION_HANDOFF.md` for the harness.
> **Not in this change:** voice-triggered barge-in. Tapping is deterministic, works on every device, and is what proves the rework end to end; the acoustic half should trigger on the **wake word**, not an energy VAD (see the handoff for why).
> **❌ The E2E tap probe is still red, and the `kotlin-stdlib` fix from 2026-08-14 never could have worked.** Same `NoClassDefFoundError: kotlin.jvm.internal.Intrinsics` at `FixtureActivity$Recorder.tapped`. The log gives the reason the earlier fix missed: the fixture runs as `Process: com.jarvis.os.test` whose `DexPathList` is **the test APK alone**, and AGP *strips from the test APK any dependency already present in the app APK* — normally right, because the test process usually shares the app's classloader; not here. So `androidTestImplementation("kotlin-stdlib")` was accepted, built green, and packaged nothing. **Fix: write `FixtureActivity` in Java** — Java emits no `Intrinsics` null-checks, so the dependency disappears instead of needing to be packaged. **The good news in the same stack trace:** `TextView.performAccessibilityActionClick` → `View.performClick` → the fixture's own `onClick`. **A real accessibility tap reached the fixture and ran its handler** — the tier's foundational unknown is answered; only the *recording* of the tap dies. Non-gating, so it has never blocked a build.

## 🪟 Control tint + wake-word decision (this branch, build pending CI)
- **Mic-off "Hey Jarvis" from any app — openWakeWord (BUILT, key-free).** Porcupine was reverted (Picovoice needs a company email), so this uses openWakeWord (Apache, no key/account). `voice/OpenWakeWord` runs the 3 bundled TFLite models (melspec→embedding→"hey jarvis", ~3.6 MB assets) — a direct port of openWakeWord's streaming pipeline, **pre-verified in Python with the real models** (1760-sample mel window → 8 frames, `mel/10+2`, 76-frame embedding window step 8, 16-embedding prediction; silence→0, noise→0.0002). `control/HotwordService` is a mic foreground service that runs it in the background and, on detection, launches the app + `engine.wakeFromHotword()`. One-owner preserved by lifecycle: engine starts the service only when backgrounded+asleep+no-session (`pause()`), stops it on `resume()` — recogniser and hotword never run together. Default ON, Settings toggle + notification Stop. **Three device-only unknowns:** true-positive threshold (scores logged for tuning), FGS background-start refusal on Android 12+ (wrapped/logged; fallback = always-running gated service), Realme's battery killer (may need a battery-optimisation exemption). Foreground wake unchanged (in-app gate + tap). Device-unconfirmed (Rule 5) — validated in Python, not on-device.
- **Translucent "in control" overlay.** While JARVIS drives the screen (an errand or an accessibility sequence), a low-alpha navy wash + cyan frame + a "JARVIS is controlling the screen" pill now covers the screen, so the taps read as JARVIS's. Built into `ScreenControlService` as a `TYPE_ACCESSIBILITY_OVERLAY` (same mechanism as the tap outline — **no new permission**), non-touchable so it never blocks JARVIS's own gestures. Shown from `executeScreen` when `plan.needsAccessibility`; cleared on `say()`, on the next `ask()`, and on the sequence's `onDone`; 30s re-armed safety backstop; removed on service unbind/destroy. Device-unconfirmed (Rule 5).
- **Wake word — BUILT (in-app asleep/awake).** Rebuilt the documented in-app model without repeating either 2026-07-26 failure: no second recogniser (the `RECOGNIZER_BUSY` cause), the "Yes?" ack reuses the ordinary speak→listen path (the partial-only cause), and no custom silence hints (the never-finalised cause). `voice/WakeWord` (pure Kotlin, JUnit-tested) matches "Hey JARVIS" — tolerant of mishears (jervis/javis/travis/"service") and a leading filler, anchored to the start so "thank you Jarvis" stays the stop phrase — and splits off any trailing command. Engine `awake` flag: asleep it ignores all non-wake speech; on wake it says "Yes?" or runs the command, stays awake 18s for follow-ups, then sleeps. A work session counts as engaged. **Tap-to-wake:** the orb wakes it while asleep, no voice needed. **Honest limit:** stock Android keeps the recogniser running while asleep — it just won't act until the wake word. A true mic-off hotword still needs a DSP engine; the key-free path is **openWakeWord** (Porcupine was built and reverted — Picovoice signup needs a company email the user doesn't have). Device-unconfirmed (Rule 5).

## 🎙️ Speech disambiguation + instructions UI (this branch, build pending CI)
From a trace where the user said "peak" and the recogniser heard "pic" four times running — even storing a wrong learned fact, `the user's playlist is called "Pic"`.

- **The n-best list was being thrown away.** `VoiceController.onResults` kept `.firstOrNull()` and never set `EXTRA_MAX_RESULTS`, so the brain only ever saw the single top guess. It now asks for up to 5 hypotheses and passes the whole ranked list through `onFinal`.
- **`voice/Transcript`** (pure Kotlin, JUnit-tested) turns the near misses into a terse `also heard: "peak", "pick"` hint — only whole different words from an equal-length hypothesis differing by one or two tokens; a different-length guess is a different sentence and is dropped. `AssistantEngine` folds the hint into the model **context** only. It stays out of the stored turn and the guards, which must match on intent, not on a maybe-word. Chosen scope was the low-friction one: the model resolves it, the user is never interrogated.
- **Instructions screen rebuilt.** Was three identical stacks of grey boxes; now three distinct blocks — editor with a full-width Save that goes green on save, learned facts with a red-edged **Forget** pill (only the pill taps now, no accidental deletes), and example chips with a leading cyan **+**.
- **Not a speech bug, noted for the user:** "play a song" offered *Zepto/Blinkit* because the user's own instruction lists them as frequent apps. And memory holds a "peak" (typed) vs "Pic" (learned) contradiction — the wrong learned fact is deletable on the new screen.

**Unverified:** recognition quality can only be judged from a device trace (Rule 5). The logic is tested; nothing here is device-confirmed and no artifact seen yet.

## 🔨 Part C tail — what a real device session found (build pending CI)
The user ran a full session and shared three traces. Six things were confirmed working; five were broken, and every fix below quotes the trace line that caused it.

**Confirmed on device** ✅ — type vs send, no spoken thought process, chats below the fold, the mic yielding while audio plays, `<<REMEMBER>>`, and PDF creation.

**Fixed** (`af37dbc`):
- **Phantom alarms.** "play Beat It" emitted `<<ALARM|TIMER|600|nap>>` and a real ten-minute timer was set; later, asked to play a song, it volunteered "what time would you like to set an alarm to wake up to this song?". `AlarmGuard` now drops alarm actions when nothing in the utterance was about time — in code beside `SendGuard`, because prompt wording is probabilistic and the rare miss costs the user their morning.
- **Asking and acting at once.** The reply asked "what app would you like to use, or should I open YouTube?" and opened YouTube anyway. `AskGuard` drops **every** action when the spoken reply ends in a question — all of them, since keeping a prefix commits to the same choice more quietly.
- **The alarm-volume check** (a direct user request). The alarm stream carries its own volume, so a turned-down phone accepts an alarm and then says nothing at the hour. JARVIS now checks after setting one and warns if it is off or under a third; "turn the alarm volume up" raises it.
- **The Files hunt** — the worst failure. Asked to open the PDF it had just made, JARVIS opened the *phone's* Files app, tapped "Starred" and "Hide Safe folder", opened an unrelated `Scriptilio4.pdf`, and told the user it had saved "Important Points" in Documents — invented. The prompt now states its files live in JARVIS's own Files screen and that it must never invent a location.
- **Reading the screen aloud** and **unresolved nicknames** — it spoke the raw screen listing back, and emitted `<<OPEN|jao>>` after learning YouTube was called "jao". Both are prompt rules now.

Still open from the same session:
- **No way to open its own artifact.** The prompt stops the damage, but there is no marker for "show me the PDF you just made". That is the natural next piece.
- **`<<OPEN|unknown>>` fails silently** — the trace shows `running Open(app=jao)` and then nothing at all. Open should report failure like taps do.
- **Execution is still not smooth.** The user's words: "look how many tries it took me to achieve the final result." Step recovery fires, but produced `Open(app=Open)` — a non-existent app name — and hunted blindly.

## 🔨 Second device session (Blinkit) — five defects, all from one trace
"can you go to blinkit and add some bread" ended with nothing done. Every fix below quotes the log line that caused it.

- **The plan was guessed before the app existed.** It aimed at `Tap(Search)`; Blinkit's box is labelled "Search for atta, dal, coke and more". Unknowable at planning time. **`AgentLoop.isErrand`** now routes open-then-interact errands through look → decide → act → look. Simple commands and follow-ups in the current app keep the fast path.
- **`Tap(Checkout)` was queued** by a request that only said "add some bread". Nothing in code would have stopped it; only an earlier failure did. **`SpendGuard`** truncates at the first irreversible tap unless the user's own words asked, and says what it withheld. Strict where `AlarmGuard` is generous — here a loose reading is what *permits* the purchase.
- **The agent budget was spent before it began** — `stepsTaken` was seeded with the whole plan, so a nine-step plan failing at step two logged `agent step 10/10`. Now counts only agent moves. (The budget was then raised to 18 in the same commit and that was itself a mistake — see the Zepto note at the top; it is **8** now.)
- **It repeated the step that just failed, three times**, while the user watched. `parseMove(reply, avoid)` refuses it in code; the prompt already forbade it.
- **Typing failed on a focused field.** The user's photo shows the search screen open, a visible caret and the keyboard up, yet `no editable field appeared`. Only `rootInActiveWindow` was searched (which can be the IME once the keyboard shows) and a node had to report `isEditable` (Blinkit evidently does not set it). Now every application window is searched, the IME skipped, and anything accepting `ACTION_SET_TEXT` counts. **Unconfirmed — this is a diagnosis, not a verified fix**, so the failure now logs window kinds and the focused node's class rather than just failing.
- **The playbook learned three routes from failed runs** — "did you are", "search box". `runSteps` now reports `ok` and `clean` separately; learning requires `clean`. **Routes already learned on the device are poisoned and should be cleared.**

Still open: **the agent cannot deliberately scroll.** Tapping a label scrolls to find it, but a product list cannot be browsed. Next piece, deliberately not stacked on top of an unverified typing fix.

## ⏸️ Part E — Commercial foundation (next; nothing built yet)
Decided in [`COMMERCIALIZATION.md`](COMMERCIALIZATION.md); §1d is the build brief.

| Piece | Status | Note |
|---|---|---|
| Cloudflare Worker `/chat` proxy | ⏸️ | Holds the key; free tier ≈100k req/day. **Testable in a Claude session** |
| D1 schema + per-user token accounting | ⏸️ | Meter **tokens, not requests** — a screen-control turn costs many times a chat turn |
| Firebase Auth (anonymous → Google) | ⏸️ | Admin SDK is Node-only and does **not** run on Workers; verify the RS256 JWT by hand |
| Android `ProxyClient` | ⏸️ | Slots in beside Groq/Gemini behind the single `Brain.generate()` |
| Play Integrity | ⏸️ | Auth answers *who*; Integrity answers *genuine install* |
| Play Billing + entitlement | ⏸️ | Purchase token verified server-side; RTDN keeps it current |

Owed by the user: a Cloudflare account, a Firebase project, and the free-tier cap
(recommended ~60k tokens/day on `llama-3.1-8b-instant`).

## Status legend
✅ done & (usually) confirmed · 🔬 shipped, awaiting on-device confirmation · ⏸️ queued, not started

## ✅ Done
- **Buildable Compose app + CI** — `assembleDebug` on every push → `jarvis-debug-apk` artifact. Public repo for free CI.
- **UI** — HUD orb (state-driven), dark Material 3 theme, Orbitron/Inter fonts, vector adaptive launcher icon, nav drawer, terminal-style Chat screen.
- **Voice loop** — always-on listen → think → speak → listen (rebuilt simple & reliable; **user-confirmed working**). Muted earcon; fresh recognizer per turn.
- **Brain** — Groq primary (llama-3.3-70b → fallbacks), Gemini fallback; **conversational-first** prompt.
- **Memory** — conversation context persisted in SharedPreferences JSON.
- **Calendar** — read real device calendar; add / delete / reschedule with confirmation.
- **Screen control** — open apps ✅; tap with cyan outline ✅; scroll via **real swipe gesture**; type + enter; **multi-step sequences** (open→tap→type→enter).
- **Speak-then-switch** — finishes the reply before switching apps (no cut-off).
- **Natural replies** — removed the robotic "is there anything else?" tic; bare "Jarvis" → "Yes?".
- **Fixed debug signing key** — committed keystore so updates install cleanly (fixed "App not installed").
- **Resume-after-Settings** — returning from the accessibility settings screen resumes listening.

## 🔬 Shipped, awaiting on-device confirmation
- Multi-step commands + typing (build #76) — e.g. "show me a standup comedy video".
- Conversational-first prompt (build #76) — JARVIS talks like a real assistant, tools when you want an action.
- Swipe-scroll for the buried-chat case (WhatsApp "open the Mom chat below the fold").

## 🧪 Testing (new — build #85)
- **Unit tests in CI** ✅ — `ScreenActions`, `CalendarActions` and `DebugLog` are covered by real JUnit tests. `testDebugUnitTest` runs **before** `assembleDebug`, so a logic regression means **no artifact**. "Artifact present" now means compiles **and** the logic is right.
- **Diagnostics screen** 🔬 — self-checks (mic, calendar, speech recognition, accessibility service, AI key) plus a **Test AI** button that does a real round-trip and reports provider + latency.
- **Shareable trace** 🔬 — every turn records heard → raw reply → markers parsed → actions run → spoken. One **Share** button exports it as text. API keys are redacted before anything can leave.
- **Typed command box** 🔬 — runs the full pipeline without the microphone, so brain/marker/tap failures can be reproduced by typing.

## ✅ Part B — continuous work session (build #89, merged)
Background-listens for follow-ups after a command opens an app; stops on "thank you Jarvis"; never listens if you only open/close JARVIS. One mic owner **by construction** — `WorkSession.owner` is a single computed value, and the foreground service never opens the mic (the engine keeps the only `VoiceController`). Play-shaped from day one: `foregroundServiceType=microphone` + persistent notification with Stop.

## 🔨 Part C — accuracy (in progress; driven by real device traces)
Shipped so far:
- **Screen awareness** (#93) — `describeScreen()` renders the live accessibility tree into the model's context (clickables in `[brackets]`, fields as `field:"…"`), so it names labels that exist instead of inventing them. Passwords and OTP-shaped digits redacted; scan bounded to 400 nodes / 45 items.
- **State memory** (#94) — when JARVIS's own UI is in front it reads the app *behind* it, and otherwise reports what the user was last in and how long ago. Previously it described its own UI and concluded it was inside JARVIS.
- **Send guard** (#95) — "type it" and "send it" are different instructions; a trailing submit/Send is dropped when the user only asked to compose. Sending is irreversible, so this is enforced in code, not only in the prompt.
- **Marker robustness** (#91) — a single `>` no longer breaks parsing, and unparseable marker text can never reach the spoken reply.
- **Executor fixes** (#91, #96) — wait for the screen to actually change after Enter; demote editable fields so the search box stops winning; don't relaunch an app that is already in front; sequences supersede each other instead of interleaving; taps report real success instead of always claiming it.

- **`<<PICK>>` mid-sequence choice** (`b922b65`) — the last blind spot. The first command of a chain planned against a screen that did not exist yet, so the model had to invent a label. `<<PICK|the first video result>>` defers the decision: at that step the executor lists what is genuinely tappable and asks the model which one, via a separate tiny call carrying none of the assistant prompt. The choice is re-found by label at tap time (node handles go stale over a ~1s round trip) and discarded if a newer command has started.
- **Open-app regression + Back/Home** (`d801260`) — screen awareness had made the model conclude it could *only* act on what was visible, so it began refusing to open apps ("I can only interact with the current app"). Opening never needed the screen. Also added `<<BACK>>`/`<<HOME>>` via `performGlobalAction`, replacing the previous hunt for a control labelled "Back".
- **Typing opens its own field** (`48d7847`) — `Type` failed if and only if the app was *not* relaunched. Not relaunching was right (relaunching threw away the user's screen) but it removed a side effect the plan leaned on: relaunching reset YouTube to its home screen, where "Search" is a real button. `Type` no longer assumes an earlier step opened a field — it taps candidates in turn until one appears, inside the existing poll budget so it still fails honestly.
- **Yields the mic to playback** (`2c062ac`) — holding the mic takes audio focus, so listening paused the very song JARVIS had just been asked to play. It now steps back while audio plays (notification offers **Talk** for one turn) and resumes on its own when the audio stops. On JARVIS's own screen it keeps listening, since the user is deliberately talking to it.
- **Learned memory** (`e2506cb`) — `<<REMEMBER|fact>>` / `<<FORGET|topic>>` let JARVIS keep durable facts it is told once ("when I say Amazon Music I mean chow", what to call the user) and follow them thereafter. Learned facts are stored apart from typed instructions so the screen can show and delete exactly what was picked up automatically. Duplicates ignored case-insensitively; at the cap the *oldest* goes, since dropping the newest would discard what the user just said. Never stores passwords, codes or card numbers.
- **Navigation restructure** (`e2506cb`) — Settings contains Voice + Appearance as sections; Chat absorbs Memory. Drawer down from eleven entries to six, all of which do something.
- **Custom instructions** (`bbe22d6`) — standing preferences appended to the model's context every turn ("call me sir", "assume IST"), fenced and framed as the user's preferences and explicitly subordinate to acting safely and truthfully. Capped at 1000 chars because they ride on *every* request.
- **Themes** (`bbe22d6`) — four palettes with the choice, persistence and `LocalAccent` plumbing real; only the accent moves until designs are decided, and the screen says so.
- **Calendar screen** (`bbe22d6`) — seven days grouped by day from the device calendar.
- **Voice picker inside the app** (`0501320`) — Drawer → Speech lists every usable voice in plain language ("British male, high quality"), auditions on tap, remembers the choice, and offers the speech-data download when the phone only has basic voices. Ranking alone was not enough: it only helps if good voices happen to be installed, and telling users to go into Android settings is not a product.
- **Home shows the real calendar** (`0501320`) — the schedule card was three hardcoded fake events that contradicted what JARVIS itself would say. It now reads the same device calendar, and tells "no permission" apart from "nothing scheduled". Fake data source deleted.
- **Alarms and timers** (`70bd645`) — via the device's own `AlarmClock` intents, so the alarm lives in the real clock app and rings whether or not JARVIS is running. JARVIS asks for the specifics first (time, morning/evening when ambiguous, whether it repeats) and reads them back. The parser refuses anything that would set the *wrong* alarm rather than approximating it.
- **A proper voice** (`aa74c4d`) — ranks every installed TTS voice instead of taking the bland default (English only, en-GB > en-US, male, higher quality, local over network) and lowers pitch to 0.92 / rate to 0.98.

## 🔨 Part F — Files (build pending CI)
**Shipped** (`f15cf54`): "make a PDF of the important points" produces a real file in the **Files** tab — open, share or delete. PDFs render with Android's own `PdfDocument` (headings, bullets, word wrap, page breaks); notes are markdown.

Two choices driven by Part E rather than by this feature:
- **No new permissions.** Artifacts live in app-private `filesDir` and are shared through a `FileProvider` scoped to that one folder — nothing for a Play reviewer to justify, nothing new on the Data safety form, and files never leave the device unless shared.
- **Image generation is refused, not faked.** Groq has no image model, so the prompt says so plainly and offers a written alternative.

Still open in Part F:
- **Flow charts / diagrams** — specced (model describes nodes and edges, app draws to a `Canvas`), not built. No new provider needed.
- **Image generation** — blocked on choosing a paid provider.

## 🎨 Part D — the six themes (build pending CI)
**Shipped** (`dd7bf2c`): the user supplied six designs and they are all in, switchable from Settings → Appearance.

Each theme owns its **geometry**, not just its palette — a crystal lattice does not become a filigree disc by recolouring it. `OrbStyle` picks the renderer:

| Theme | Centrepiece | Motion |
|---|---|---|
| Arc Reactor | cyan + gold energy bands | four radii churning in opposite directions |
| Lattice | six crystal prisms on a gold-trace hexagon | turns as one rigid piece |
| Prism | faceted gem inside a node dome | gold vortex spiralling inward |
| Forge | ornate copper filigree, molten core | six rings counter-rotating at different speeds |
| Core | gear ringed by crystal shards | gear and hub turn opposite ways |
| Nebula | crystal flower over a starfield | four spiral dust arms sweeping |

- **Everything moves**, which was the brief. Three independent clocks (fast spin, slow drift, counter-rotation) plus a breathing pulse feed every style, so each has its own character rather than one shared rotation. Live mic amplitude widens strokes and brightens cores. **Thinking** speeds every clock up — that is the state where the user is waiting.
- **Backdrop + waveform** — every design puts the orb in a space, not on flat black: a wash, a drifting dust field and the faint arc of a dome. The waveform strip under the orb is driven by the real mic level, so it shows whether JARVIS can currently hear you.
- **Live previews** — the picker runs the real renderer in each card. Three themes differ mainly in how they *move*, so still swatches would have shown six near-identical circles.
- **Switching moves the whole app** — `JarvisTheme` derives the colour scheme from the palette and provides both composition locals; HomeScreen's hardcoded cyan is gone.
- **Tested:** particle placement is a pure function of a seed (never `Math.random` — a Canvas redraws every frame, and a re-rolled starfield is static, not stars). Determinism, range, distribution, spacing and divide-by-zero guards are covered, plus palette tests for id round-tripping, unique styles and contrast floors. Old ids (`ember`, `signal`, `violet`) fall back to the default.
- **Redrawn to match the art** (`bdc3489`) — the first pass was a mock and the user said so. What was missing: **texture** (the rings in the designs are fine *dotted light strips*, not strokes — now dashed strokes, one draw call each, plus the dense radial hatching that makes filigree read as filigree rather than a dial), **depth** (four designs sit inside a real wireframe globe — latitudes bunching at the poles, meridians narrowing to the centre — plus a strut-and-ball geodesic shell for the node-built ones), and **light** (four-point lens flares with a white specular core; energy bands that glow with falloff instead of being flat arcs). Also: gold **circuit traces with right-angle bends** for Lattice, **crystal facets showing internal cut lines**, and a **perspective ground mesh**. A reusable `OrbPrimitives` vocabulary now backs all six styles.
- **The wordmark** (`bdc3489`) — "JARVIS / SYSTEM" over the orb centre, the anchor every reference image has and the app did not. Cut from a vertical gradient (white specular → the theme's metal → its secondary) with a zero-offset coloured shadow as glow. Orbitron already ships and is the same squared face the designs use. Each palette names its own metal, since deriving it from the accent got it wrong.
- **Status block** — uppercase and widely tracked like the artwork, but showing JARVIS's **real** state. A line permanently reading "COMMAND ACCEPTED" would be decoration lying about what the app is doing.
- **The artwork IS the orb now** (`3284ed3`) — the user's screenshot settled it: "how do any of these resemble the images???" They did not. Lattice was scattered crystals joined by a staircase of traces, Prism a spiky star rather than a faceted gem, Forge a sunburst of sticks. **The approach was wrong, not the execution:** the references are photorealistic renders and vector shapes on a Canvas cannot reach them, however many are added — two passes went into proving that. Each design is now cropped to its orb, scaled to 720px and shipped as WebP with a radial alpha falloff so it floats rather than showing a square edge. **641 KB for all six**, crops framed to exclude the status text baked into the source images.
- **~~The whole sprite approach~~ — rejected, and rightly** (`5a18bbf`). Sliced bands sheared into hard-edged wedges: *"the rings don't look natural… absolutely horrible. Can you not make proper 3D rings."* A flat photograph has no depth to turn through, so no tuning could fix it.
- **The orb is real 3D geometry** (`5a18bbf`) — each ring is a circle in three dimensions with its own tilt, precessing and spinning on its own multiple of one master clock, projected through a **perspective** camera. Three things make it read as depth: perspective (the near side projects larger — orthographic looks flat), **depth shading** (each of 24 chunks per ring takes brightness and width from its own Z, so rings pass in front of and behind the core), and **additive light** (`BlendMode.Plus`, so crossing rings bloom — alpha compositing cannot fake it). Plus dust on a Fibonacci sphere, crystal shards, hub spokes, and a mic-driven core.
- **The images are gone** (`5a18bbf`) — 780 KB of drawables deleted, with the cropping, alpha masks and text inpainting they needed. Procedural geometry scales to any size, so the picker previews are the real orb rather than a shrunken bitmap.
- **~~Motion sits around the art~~ — rejected, and rightly** (`cee0dbf`). Rings drawn *outside* the artwork orbited a picture that never moved. The user: *"the objects in the image should move instead of you adding extra moving elements outside the image."*
- **The artwork's own rings rotate** (`cee0dbf`) — each sprite is clipped into concentric bands and each band turns at its own speed, so the design's real rings move against each other. Band edges are cut where an artwork already has a gap between rings, so a seam lands where the eye reads a boundary anyway. Per-theme profiles: Filigree four bands (it is all fine rings), Lattice only two turning nearly together (a hexagon of prisms shears apart otherwise), Machine's gear against a near-still dome.
- **The wordmark is drawn in code again, centred** (`cee0dbf`) — baked into the art it ran wider than the circular fade, so it was clipped on the right and read as off-centre; and it could not survive being sliced and spun with the bands.
- **Text removed from the artwork by rotational cloning** (`cee0dbf`) — these designs are concentric, so a covered pixel's own radius at another angle holds the right content. **Mirroring across the horizontal axis was tried and rejected**: it turns the lower half into a reflection and every theme grows a lens-shaped artifact. Where a radius is covered at every angle the repair walks outward for one that is not, because falling to black left a dead spot in Arc.
- **Deleted rather than left to rot:** the dotted rings, orbiting dust and flares that surrounded the orb, and the `OrbMath` geometry helpers only they used. What survives is what the backdrop still needs — deterministic star placement, star flares, ground mesh.
- **⚠️ Pre-publish gate:** the wordmark is **baked into the artwork**. Renaming the app — which the Marvel trademark issue may force — means commissioning new art, not a code change.
- **Not verified here:** how it *looks* and whether it holds framerate — no emulator or SDK in this environment. CI compiling is the ceiling, and the user is the only one who can judge the resemblance.
- **Flagged:** the Avengers mark in one reference image was deliberately not reproduced. Marvel IP, and with the "JARVIS" name it is a real takedown risk — already a pre-publish gate in `COMMERCIALIZATION.md`.

## 🎨 Themes — five rewrites, and where it landed
The user supplied six reference renders. Getting them into the app took five attempts, and the sequence is the point:
1. **Hand-drawn vector shapes** (`dd7bf2c`) — six orb geometries drawn on a Canvas. Verdict: a generic sci-fi HUD, not the designs.
2. **Higher-fidelity vectors** (`bdc3489`) — dotted light strips, wireframe globes, lens flares, circuit traces, a metal wordmark. Closer, still not it: *"how do any of these resemble the images???"*
3. **Ship the renders as sprites** (`49a5128`) — an exact likeness, because it WAS the image. But a still image cannot move, so rings were added orbiting outside it. Rejected: *"the objects in the image should move instead of you adding extra moving elements outside."*
4. **Slice the sprites into rotating bands** (`af87cb9`) — the artwork's own rings turned. They sheared into hard-edged wedges: a flat photograph has no depth to rotate through. *"Absolutely horrible."*
5. **Real 3D geometry** (`632d595`, `0f54e35`) — where it now is. Rings are circles in space, tilted and precessing, projected through a perspective camera, depth-shaded, additively blended. Each is a **filled luminous band** (inner + outer edge, foreshortening correctly) rather than a stroked wire. Every theme has **its own backdrop** — wireframe globe, geodesic shell, warm haze with HUD brackets, nebula over a circuit floor. **Ring colour is measured from the references**, not chosen: the brightest quartile of each annulus, sampled at ten radii, wordmark masked out.

**The constraint, stated plainly:** the references are photorealistic renders. 100% resemblance *without* shipping them is not achievable — it would mean authoring the exact 3D scene that produced each image, which cannot be recovered from a 2D render. The choice is exact-but-static (ship the images) or procedural-and-alive (this). One option remains untried: texture-map the renders onto the 3D ring geometry via `drawVertices`, which would move correctly AND look like the source, at the cost of shipping the images as textures. Offered, not yet chosen.

## ⚡ Settings performance (build pending CI)
**Reported by the user:** "the app starts lagging when I go into the setting section."

Measured before changing anything: the theme picker draws **six live 3D orbs at once**, costing ~2,068 draw calls and **~14,628 object allocations per frame** — 880,000 allocations a second at 60fps, which is GC thrash. Three independent causes:
- **Per-frame allocation.** Every ring rebuilt its geometry into fresh Lists: `Orb3D.ring()` allocates a List + a `Vec3` per point, and `.map { project(it) }` a second List + a `Projected` per point — ~580 objects per ring per frame. `Orb3D.ringInto` now writes `x/y/depth` into a `FloatArray` held across frames, with the two rotations and the projection inlined. **A test asserts it matches `ring()` + `project()` to the float**, because a hand-inlining slip changes the orb's shape silently rather than failing.
- **No detail scaling.** A 92dp preview was drawn at the same 96 segments / 24 chunks as the 280dp home orb. Detail now follows size (`OrbQuality`).
- **Off-screen work.** All six cards composed and animated in a scrolling `Column`; now a `LazyColumn`.

**After: ~345 draw calls a frame, no per-frame allocation.** The home orb is unchanged (still `High`).

**The trap avoided** is why `OrbQuality` is a separate type from `OrbDetail`: a selected card animates its orb 88dp↔104dp, so keying the buffers on raw size would rebuild them every frame of that animation — reintroducing the exact problem. A test walks 88→104dp in half-dp steps asserting the band never moves.

## 🔨 Part C tail — self-correction and a quieter reply (build pending CI)
- **Recovers from a failed step** (`53fc4e0`) — when a step fails, the executor no longer gives up on the whole sequence. It hands the model the reason it failed *and* a fresh reading of the live screen, and runs the replacement it comes back with (max two recoveries per sequence, so a wrong plan cannot loop). This is what "sense that something is not playing and figure it out" needed: the plan is rewritten from what is actually on screen, not retried blindly.
- **Model routing reverted** (`53fc4e0`) — routing commands to the small model was tried and measured over three device traces: it returned **no markers on every single command**, so each fell through to the smart model anyway. That doubles requests rather than halving them. `tierFor` now always returns SMART, with the evidence recorded at the call site. The fast tier stays where it demonstrably works — the `<<PICK>>` chooser and the Diagnostics ping, both ten-token prompts answering with one value.
- **Red build fixed** (`6f5043d`) — **CI had been failing since `53fc4e0`, four commits back, and it was not noticed.** Two `ModelRouterTest` cases still asserted commands route to `Tier.FAST` after that commit deliberately stopped doing it: 149 tests, 2 failed, so `testDebugUnitTest` gated `assembleDebug` and **no APK was produced for four commits**. The code was correct; the behaviour changed and its tests did not. Also deleted what the revert orphaned — the command-verb list, conversation cues, word-count bound and `expectsAction` were all unreachable once `tierFor` became a constant, and leftover routing heuristics imply the app still routes.
- **One system prompt, 41% smaller** (`4ad64b8`) — it was pasted into `GroqClient` **and** `GeminiClient`, two byte-identical copies free to drift, and both carried the same bug: a literal `\n` inside a Kotlin **raw** string, where `\n` is not an escape. The model had been reading the characters backslash-n since Files was added. Now one `SystemPrompt.kt` in the package. Also the diet owed since the rate-limit diagnosis: **9,196 → 5,421 chars (~2,299 → ~1,355 tokens)**, charged on *every* request — at the old size it was over half of Groq's 12,000 tokens-per-minute allowance by itself. What went is the prose explaining *why* each rule exists (moved to a KDoc, where it costs nothing per request), three restatements of the same "only claim what you emitted" rule, and a stray calendar sentence that had drifted into the alarm section. **Four tests** pin it: no literal `\n`, a length ceiling, every parseable marker is taught, and the eight rules earned from device failures are still present by phrase.
- **JARVIS no longer narrates its thought process** (`774b5cf`) — it was speaking "Here are the steps: ." aloud. The first attempt only tidied the punctuation, which is why the user saw it again. A clause ending in a colon is, in practice, always the model announcing the markers it is about to emit; since the markers are stripped, that narration describes nothing, so it is now **removed entirely** — and only when the reply actually carried markers, so "There are two options: tea or coffee." keeps its punctuation.

Still open in the UI (Part D):
- **Files** and **Automation** — the only remaining placeholders; **awaiting the user's spec**.
- ~~Themes needs designs~~ — **done** (`dd7bf2c`), see below.
- Resolved: Settings now contains Voice + Appearance, Chat absorbed Memory, and Vision/Skills were removed for having nothing behind them.

Still open in Part C:
- **The Thriller-album tap** — reported false success for four attempts; now reports honestly, but the underlying cause is not yet identified.
- Tap verification + retry; disambiguation when matches tie.

## ⏸️ Queued (not started) — see [`EXECUTION_PLAN.md`](EXECUTION_PLAN.md)
- **Part D** — polish (tap-to-talk toggle; clear idle text; permission onboarding).
- **Part E** — commercialization: key security (proxy + BYOK), Play compliance cleanups, release AAB pipeline, name/`applicationId` gate, billing, launch. See [`COMMERCIALIZATION.md`](COMMERCIALIZATION.md).
- Later — proper wake word (Porcupine/openWakeWord); streaming replies; device skills (alarms/timers, SMS/calls, toggles, media); vision.

## ⚠️ Groq rate limits — it is TOKENS per minute (measured 2026-07-28)
The Groq dashboard settled it: requests peaked at **19 against a limit of 30**, but total tokens peaked at **~11.5K against ~12K**. The cap being hit is **tokens per minute**, so the fix is smaller requests, not fewer.

Measured cost per request before the fix:

| Part | Tokens |
|---|---|
| System prompt | ~2,000 |
| Screen description | ~300 |
| Date + calendar | ~120 |
| 20 turns of history | ~800 |
| **Total, every request** | **~3,200** |

At 12,000 TPM that is only **3–4 commands a minute**. The system prompt grew from ~1,100 to 2,001 tokens across one session, a paragraph at a time, and nothing ever measured the total.

Fixed in `2309d22`: `Test AI` no longer sends the whole assistant prompt to hear "OK" (~2,000 → ~20 tokens), and history is halved (20 → 10 turns, ~400 tokens off every request).

Fixed in `c04c7de`: **Groq's quotas are per MODEL.** A device screenshot showed `llama-3.3-70b-versatile` out of daily tokens (`Limit 100000, Used 98444, Requested 2674`) while the smaller models still had their own untouched allowance — and the client gave up anyway, because 429 did not fall through to the next model the way 404 did. Now it does, cooldowns are tracked per model, and only an all-models-limited state fails.

Fixed in `f73b3e3`: **commands are routed to `llama-3.1-8b-instant`**, keeping the 70b allowance for turns that need thinking. Conservative — an explicit request to think wins over a command verb, long utterances are conversation, unknown input goes smart. If a command produces *no* marker at all, the turn is retried once on the smart model, since the marker protocol is fiddly enough for a small model to fumble. The trace records which tier answered.

Fixed in `ec549cb`: **`gemma2-9b-it` was retired by Groq** and the lists still named it — replaced with the current production models (`openai/gpt-oss-20b` fast, `openai/gpt-oss-120b` smart, alongside the two llamas). Worse, **a retired model aborted the whole request**: Groq reports retirement as HTTP **400**, not 404, and only 404/429 fell through — so with the 8b rate limited and gemma2 dead, the chain stopped at the dead model while a working 70b sat next in line. The fallback chain existed and could not be reached. Retirement is now matched on the message text rather than the status code, and the model is dropped for the life of the process.

That screenshot also confirmed the arithmetic: `Requested 2674` against 100,000/day is **~37 commands per day** on the 70b alone.

**Still open — the biggest remaining win:** a careful editing pass on the system prompt itself, ~2,000 → ~1,200 tokens, which would roughly double the commands-per-minute headroom. Left undone deliberately: trimming prompt text carelessly is how the behaviours fixed this session regress.

## ⚠️ Groq rate limits (hit on-device, 2026-07-28)
A Diagnostics trace showed one good round-trip then **25 rate-limit failures in ~30s**. Two fixes shipped in `604d07f`: the client now surfaces Groq's own message (which limit — requests/minute vs tokens/day — and when it clears) instead of a generic "wait a moment", and it **refuses locally until the limit clears** rather than spending more rejected requests against the same quota.

Worth knowing: **Groq's limits are per account, not per user.** This is the single-user preview of the scaling problem in [`COMMERCIALIZATION.md`](COMMERCIALIZATION.md) — with a shared key behind a proxy, every user hits it simultaneously. Part E1 is the fix.

## Known limitations / caveats
- **No reliable always-on wake word** — stock `SpeechRecognizer` can't do it; needs a dedicated hotword engine.
- **Screen control is best-effort** — it reads the accessibility tree, which varies per app; occasional misses are expected (no real API for other apps).
- **Claude can't test live LLM replies** — Groq is egress-blocked (403) in the build environment, and the key is a build secret. Reply quality is verified on-device by the user; Claude verifies everything mechanical.
- **CI status API lags 2–5 hours** right now — the **presence of the `jarvis-debug-apk` artifact is the reliable "green" signal** (not the job status).

## ✅ Playbook — remembering the routes that worked (build pending CI)
`4ef0a7f`. A screen sequence that runs clean is stored; a matching request replays it **without calling the model** — instant, no tokens against the per-minute limit, and the sequence that demonstrably worked rather than a fresh guess.

- **The slot is what makes it useful.** "play Beat It on YouTube" is stored as `play * on youtube`, so "play Thriller on YouTube" matches and replays with the new text. A literal cache would never fire; nobody says the same sentence twice.
- **The variable part is found, not guessed** — whatever the sequence TYPED is by definition what varies, and only if that text also appeared in what the user said. Otherwise the route is stored literally.
- **Steps are stored as marker strings**, so replay round-trips through `ScreenActions.parse` — the tested parser, not a second format. Consequence found in pre-flight: `<<PICK>>` descriptions template too, so asking for Thriller picks *the first Thriller video*.
- **Safety.** Replay acts *without* the model, on a fuzzy match, so routes containing anything irreversible (send, buy, order, pay, call, delete) are **never learned**, and a slot value that is itself such an instruction refuses the match. Only clean runs are stored — a sequence that needed recovery is exactly the one whose steps were wrong. Routes are per-install and never leave the phone.

Still open: the routes are not yet visible or deletable in the UI.

## Feature status table
| Feature | Status | Build | Notes |
|---|---|---|---|
| Buildable app + CI | ✅ | — | artifact `jarvis-debug-apk` |
| HUD orb UI / theme / fonts / icon | ✅ | — | |
| Always-on voice loop | ✅ | rebuilt | user-confirmed |
| Groq brain + memory | ✅ | — | Gemini fallback |
| Calendar read/add/del/reschedule | ✅ | — | real device calendar |
| Open app | ✅ | — | confirmed |
| Tap control + outline | ✅ | — | confirmed (YouTube) |
| Scroll to find (swipe) | 🔬 | #73 | test the Mom-chat case |
| Type + Enter / search | 🔬 | #76 | |
| Multi-step chained commands | 🔬 | #76 | "standup comedy video" |
| Conversational-first replies | 🔬 | #76 | |
| Fixed signing (clean updates) | ✅ | #52 | |
| Unit tests gating CI | ✅ | #85 | parsers, DebugLog, WorkSession, SendGuard |
| Diagnostics + shareable trace | ✅ | #85 | drawer → Diagnostics; traces now drive the fixes |
| Typed command box (no mic) | 🔬 | #85 | full pipeline test |
| Continuous work session | ✅ | #89 | one mic owner by construction |
| Screen awareness (AI reads the screen) | 🔬 | #93 | needs on-device confirmation |
| State memory (app behind JARVIS) | 🔬 | #94 | |
| Type vs send kept separate | 🔬 | #95 | guard + prompt |
| Sequences supersede, honest taps | 🔬 | #96 | |
| Mid-sequence choice (`<<PICK>>`) | ✅ | pending | device-confirmed: chose "Beat It Michael Jackson" from 15 real options |
| Back / Home via global actions | 🔬 | pending | replaces hunting for a "Back" label |
| Open-app refusal regression | 🔬 | pending | it had stopped opening apps entirely |
| Better TTS voice | 🔬 | pending | ranks installed voices; premium voice is a Part E perk |
| Typing opens its own text field | 🔬 | pending | no longer depends on <<TAP\|Search>> having worked |
| Yields the mic while audio plays | 🔬 | pending | song no longer stops; notification offers Talk |
| Alarms and timers | 🔬 | pending | device clock app; asks for specifics first |
| In-app voice picker | 🔬 | pending | no trip to Android settings |
| Home reads the real calendar | 🔬 | pending | replaced hardcoded fake events |
| Polish (toggle/onboarding) | ⏸️ | — | Part D |
| Key out of the APK (proxy + BYOK) | ⏸️ | — | Part E1 |
| Play compliance + release AAB | ⏸️ | — | Part E2–E3 |
| Subscription billing | ⏸️ | — | Part E5 |
| Playbook — replay routes that worked | 🔬 | #177 | slot templating; never learns irreversible routes |
| Agent loop — one step at a time | 🔬 | pending | replaces blind recovery; budget 10; stops before irreversible |
| Files tab (PDFs + notes) | 🔬 | pending | app-private, no new permission |
| Recovers from a failed step | 🔬 | pending | replans from the live screen, max 2 tries |
| No spoken thought process | 🔬 | pending | narration clauses stripped, not tidied |
| Playbook: replay routes that worked | 🔬 | pending | slot templating; irreversible routes never learned |
| Six themes, orb built in 3D | 🔬 | pending | tilted rings, depth-shaded, additive |
| Artwork shipped as the orb | 🔬 | pending | exact replicas; 641 KB for six |
| Animated overlay round the art | 🔬 | pending | rings, travelling arc, dust, flares |
| Theme-driven background/surfaces | 🔬 | pending | not just the accent any more |
| Backdrop + mic-driven waveform | 🔬 | pending | from the designs |
